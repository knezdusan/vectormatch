## The Context
The web development job market faces a profound crisis as rapid AI advancement increasingly reduces the demand for human developers. Traditional job search platforms like LinkedIn and Freelancer.com, and others which were the primary hubs for job discovery are now oversaturated, with hundreds of developers competing for each open position. This saturation makes it nearly impossible for qualified candidates to stand out or find opportunities that genuinely match their skill-sets. VectorMatch.dev addresses this challenge by providing an AI-powered alternative that intelligently matches developers with relevant job opportunities based on their actual experience and technical capabilities.

## Mission & Concept
VectorMatch.dev bypasses traditional HR bottlenecks by connecting software engineers directly to major Applicant Tracking Systems (ATS) platforms like Greenhouse, Lever, and Workday. Instead of competing in oversaturated job boards, developers upload their CVs which are analyzed by AI to extract chronological experience and technical skill-sets. This structured data powers intelligent matching against live ATS job postings, enabling developers to discover and apply for opportunities that precisely match their capabilities—before they ever reach human recruiters. 

## Breakdown of the Application Technical Implementation
The TDD (Technical Development Design) system is architected into four distinct modules:
- **Module A: Developer-Centric Onboarding (Frontend & API)**

  Converts raw, messy developer resumes (PDFs) into structured, validated data using client-side extraction and a specialized server-side AI overlap merge algorithm to accurately calculate years of experience. It also handles the "5 Major Skills" drag-and-drop constraint to create a clean user persona without muddy vector overlap.  

- **Module B: Seeding & Ingestion Engine**

  An automated discovery engine for raw, untapped corporate data sources.
  Discovers job boards and candidate companies for $0 by pulling from public data sets like Google BigQuery, Hacker News, SSL certificates, Reddit, Wayback Machine, YC Directory, VC portfolios, and more. It feeds these discovered targets into a rate-limited background worker (the "Phalanx" Poller) to fetch live job postings. The corpus expansion pipeline (June 29 2026) expanded the company registry from 449 to 5,290 companies via 10 batch sources + 13 daily-native sources, all verified for free-tier compliance. The corpus-persona alignment session (July 7 2026) further expanded the registry to 10,114 companies and added frontend-targeted discovery sources (GitHub Events Probe, Brave Search frontend queries, Frontend Job Scanner) to align the corpus with the mission of serving frontend/web developers seeking remote startup roles. Key architectural pivots: G5+G6 batch polling (Inngest 50K exec limit), G7 rawJson pruning (Neon 512MB limit), Flush-and-Flow architecture, v2 remote-scope extraction (deterministic regex + LLM fallback), company scoring matrix (employee count + agency/public/source-origin signals), 5-tier circuit breaker, and a three-horizon freshness/retention model (30-day injection cap, 60-day active-job age boundary, 90-day hard delete). See `docs/reports/CORPUS_EXPANSION_TDD.md` for the full implementation TDD and `docs/reports/CORPUS_ALIGNMENT_SESSION_HANDOFF.md` for the alignment session report.

  **Direct Job Board Ingestion (July 2026):** A parallel ingestion path that bypasses the ATS poller entirely, fetching jobs directly from remote-first job boards with structured APIs. Six boards are wired: Himalayas (103K+ jobs), NoFluffJobs (11,446 jobs, 9,320 remote — CEE/EU focus with structured tech tags, salary, seniority), RemoteOK, Arbeitnow (EU-focused), Remotive, and WeWorkRemotely (RSS). Each adapter transforms board-specific JSON/XML into a unified `DirectIngestionJob` shape, applies a persona tech-stack filter (50+ frontend/PHP/Laravel keywords with word-boundary matching), and upserts directly into the `job` table with structured fields + embeddings + `normalizedAt` set — skipping Gate 0, normalization, and Gate 0.5 entirely. The `directJobBoardIngestion` Inngest function (cron `0 5 * * *`, concurrency 1) orchestrates all 6 boards in sequence. A backlog sweeper (`pollBacklogSweeper`) addresses the 6,516 never-polled companies (65% of the 10,114 registry) by polling 100 never-polled companies per run. See `docs/system/direct-ingestion-compensation-handoff.md` for the full implementation handoff.

  **Wellfound + FlareSolverr (D13-D20, July 2026):** Wellfound (1,889 remote software-engineer jobs) is Cloudflare-walled — HTTP fetch returns a captcha page. FlareSolverr 3.5.0 is deployed as a Coolify service for Cloudflare bypass. The Wellfound adapter (`src/lib/jobs/direct-ingestion/wellfound.ts`, 473 lines) tries FlareSolverr first (cheerio HTML parsing), falls back to Playwright. Verified: Cloudflare bypass on Wellfound confirmed (112KB HTML). `FLARESOLVERR_URL=http://flaresolverr-v104gdwm9iidiajuwd2jy52t:8191/v1` set in the app's Coolify environment. See TDD §7.0e for FlareSolverr deployment details.

  **SpaceX False-Global Regression Fix (D14, July 2026):** 46 SpaceX jobs classified as `global` despite being onsite-US. `extractRemoteScope` fixed to correctly handle `workplaceType='remote'` + `isSpecificLocation` as `country_fenced`, not `global`. Prevents the multi-probe from confirming `global` in conflict cases where the location is a specific US city. 46 SpaceX jobs + 3 similar jobs from other companies reclassified to `country_fenced`, embeddings nulled for re-processing.

  **Embedding Symmetry Enforcement (D20, July 2026):** 76% of jobs (2,488 of 3,281) had no embedding. 18 active unfenced jobs were invisible to Gate 2. 520 fenced jobs had wasted embeddings. Fixed: 18 active unfenced jobs embedded using `text-embedding-3-small` (~$0.001 OpenAI cost), 520 fenced jobs' embeddings nulled (fenced jobs can never match). Verified symmetry: 0 fenced with embedding, 0 active unfenced without embedding. Root cause: the D19 fence backfill (which set `is_fenced=true` on 2,926 jobs) didn't null the embeddings, and 18 recently-ingested jobs failed to embed during normalization (transient OpenAI API errors).

  **BigQuery MCP Integration**: Uses Google BigQuery MCP server (configured in `.devin/config.json`) for AI-assisted public dataset analysis. See `docs/reports/bigquery-mcp-setup.md` for setup and usage details. Leverages BigQuery free tier (1 TB/month, requires billing-enabled project but costs $0 at MVP scale). The optimized query uses the Wappalyzer `technologies` column (~15 GB/scan) instead of the `payload` JSON column (~4 TB/scan) — a 270x cost reduction. GCP credentials are passed via `GOOGLE_APPLICATION_CREDENTIALS_B64` (base64-encoded JSON, Docker-safe for Coolify).

  **Corpus Expansion Pipeline (June 29 2026):** 10 batch sources (B1-B10) for one-time flush + 13 daily-native sources (D1-D13) for continuous discovery, all wired into Inngest with staggered cron triggers. Batch flush executed successfully: 5,290 companies (106% of 5,000 target). Top source: Wayback CDX (4,163 companies). Two sources disabled: D1/B2 Google CSE (API discontinued for new customers — replaced by Brave Search API in Sprint 3) and B8 Rapid7 FDNS (commercial licensing required — D6 CertStream covers same approach). Workable API schema drift fixed (June 2026 API revision: `company.name`→`company.title`, `company.shortName` removed, slug extracted from `company.url`). See `docs/reports/CORPUS_EXPANSION_HANDOFF.md` §"Post-Flush Update" for full details.

  **Corpus-Persona Alignment (July 7 2026):** Aligned the job corpus with the mission of serving frontend/web developers seeking remote startup roles. Company registry grew from 5,290 to 10,114 companies. Key changes: (1) P0-1 company scorer backfill — 932 companies promoted to `active_hot`, 9,126 to `active`, 36 to `dormant`, 2 to `dead`; 10 defense companies demoted. (2) P1-1 employee count backfill — 1,431 YC/VC-sourced companies populated with estimated employee counts (YC: 30, VC: 100). (3) P0-3 remote-scope backfill — 3,217 jobs classified via v2 remote-scope extractor (deterministic regex + LLM fallback, ~$0.71 cost). (4) P1-2 GitHub Events Probe — 129 frontend-ecosystem orgs tracked for new ATS-bearing repos. (5) P1-3 Brave Search frontend queries — 6 frontend-targeted search queries added. (6) P2-2 Frontend Job Scanner — new daily source scanning Greenhouse/Lever/Ashby job boards for frontend-keyword postings via Brave Search. (7) P2-1 BigQuery seeder re-run — 3,778 domains found, 448 slugs resolved, 0 new companies (corpus saturated). (8) Backlog normalization — 1,237 jobs normalized (1,104 success, 109 rejected, 24 failed on >8192 token limit). See `docs/reports/CORPUS_ALIGNMENT_SESSION_HANDOFF.md` for the full session report.

- **Module C: Event-Driven Routing (The 3-Gate Funnel)**

  The core matching brain. It processes incoming jobs by indexing them and running an optimized, ultra-fast 3-gate funnel (Exact Filtering $\rightarrow$ Semantic Vector Similarity $\rightarrow$ Parallel High-Fidelity LLM Evaluation) to match jobs to users. Includes a Gate 0.5 hard-blocker pre-filter (geo-fencing, compensation, experience), seniority-aware matching, A/B test prompt variants, work authorization filtering + risk flagging (EU citizenship, RWR Card Plus, Blue Card EU, UK settled status, US green card, etc.), a Gate 3 feedback loop (re-evaluates rejected matches when persona tags change), and a pending-queue sweep for resilience. **Gate 0.5 hardening (July 2026):** hybrid jobs in foreign countries are now hard-blocked when `remoteScope != 'global'` and the location doesn't match the applicant's country; null `workplaceType` jobs with specific city locations (e.g., "Pune, MH, in") are blocked when the location has no remote indicators and doesn't match the applicant's country. **Gate 3 prompt hardening (July 2026):** all 3 prompt variants now explicitly distinguish full-time/W-2 roles (work authorization is a hard blocker) from contractor/B2B roles (work authorization is advisory only, not a blocker); hybrid in a foreign country is a hard blocker (not soft) unless in the applicant's country/region; null `workplaceType` with a specific city location is inferred as on-site (not "maybe remote"). **LLM safeguards (July 2026):** Gate 3 LLM calls use a 30-second timeout via `AbortSignal.timeout(30000)` to prevent indefinite hangs; job description input is truncated to 8,000 characters to prevent token-limit failures on oversized postings. **Mismatch analysis + 6-fix cascade (July 2026):** Analysis of 50 user-marked mismatches revealed a 5-root-cause cascade. (1) 88% of mismatches had NULL embeddings — the `runGate1Only` fallback let unembedded jobs bypass Gate 2 entirely, matching on 1-tag overlap alone. Fixed by returning `[]` for empty embeddings (jobs wait for the embedding backfill). (2) 32% had `overlap_score = 1` — a single shared tag let weak matches through. Fixed by adding `GATE1_MIN_OVERLAP = 2` (env-configurable) to the Gate 1+2 SQL query. (3) 30% were non-US country-fenced jobs (CA, PL, PT, MX, IN, CL, CR) approved by Gate 3 because the compliance directive's "DEFAULT TO APPROVING" language was applied to ALL countries, not just US. Fixed by restructuring the directive into a 2-step check: Step 1 hard-blocks non-US country-fenced jobs, Step 2 applies the US-only approval rule. (4) 12% were management/PM roles (Engineering Manager, Product Manager, etc.) approved for IC-only personas. Fixed by adding criterion 8 (management/PM role detection) to all 3 prompt variants. (5) 4% had `remote_scope = 'global'` with `location_countries = ['US']` — a contradiction that let them bypass Gate 0.5 Check 8 (which skips global) while Gate 3 saw a US-only restriction. Fixed by forcing `locationCountries = null` when `remoteScope = 'global'` in all 3 handler paths. (6) Gate 0.5 Check 8 soft-fail-opened for country-fenced jobs when the JD was silent on work auth — but for non-US countries, the absence of fencing language doesn't mean the job is open to non-residents. Fixed by hard-blocking country-fenced jobs when the list doesn't include the US (pattern `country_fenced_non_us`), regardless of fencing language. Combined, all 50 mismatches are addressed: 48 of 50 at the Gate 1+2 / Gate 0.5 level (before LLM cost), 2 at the Gate 3 level. See TDD §5.2-5.3 for full implementation details.

- **Module D: Business Logic & Compliance (Go-To-Market)**

  Generates automated, personalized, peer-to-peer cold outreach emails ("Minute Zero" pitches) for developers to send directly to CTOs, and bundles them with a B2B contract compliance legal sheet (W-8BEN instructions) to lower the corporate friction of hiring independent contractors.  

- **Module E: Infrastructure & Deployment Architecture**

  Handles the complete deployment infrastructure using self-hosted PaaS (Hetzner Cloud CX33 + Coolify) with Docker-based Next.js standalone output, Neon PostgreSQL in Frankfurt for minimal query latency, and Cloudflare edge protection for critical API endpoints. Deployed and running (healthy) as of June 2026. Includes a lazy-initialization pattern for build-time safety (no runtime secrets needed at Docker build time), operational constraints around filesystem caching with horizontal scaling, and requires manual maintenance through the Coolify dashboard.

## Status Designation Legend
To maintain absolute alignment between this blueprint and the active codebase, every component, module, and table is labeled with one of the following statuses:
- **`[Status: Implemented]`**: The feature or component is fully written, integrated, and active in the current codebase.
- **`[Status: Partially Implemented]`**: The UI structure, routing, or database placeholders exist in the codebase, but core matching logic, background jobs, or third-party integrations are not yet wired up.
- **`[Status: Planned / TO DO]`**: The feature or module is technically architected but has not yet been introduced to the codebase.
- **`[Status: Deprecated — Retained Historically]`**: The code/table exists in the codebase but has been superseded by a newer architectural decision. It is intentionally kept (e.g. to preserve migration history) and must not be referenced by new application code.


## Build Sequence `[Status: Tracking]`

Bottom-up order for solo implementation. Update the status tag as each step completes.

1. **Database & Schema Layer** `[Status: Implemented]` — Drizzle schemas for `applicant`, `job`, `persona`, `matchQueue`, `cvUpload`, `workingHistory`, `tagsExperience` with GIN + HNSW indexes. (Completed in schema-cleanup session + Module A schema phase, June 2026.)
2. **Module A Contracts** `[Status: Implemented]` — `CANONICAL_TAGS` (144 entries, `src/lib/jobs/tech-tags.ts`), `CANONICAL_ROLES` (~90 entries, `src/lib/jobs/roles.ts`), `resumeExtractionSchema` + `onboardingPayloadSchema` Zod schemas (`src/lib/onboarding/schemas.ts`). (Completed in Module A contract phase, June 2026.)
3. **Client-Side PDF Extraction & AI Parsing** `[Status: Implemented]` — `pdfjs-dist` in main-thread "fake worker" mode (`src/lib/onboarding/pdf-worker-client.ts`) + `generateObject` call against `resumeExtractionSchema` (Schema 1) with dynamic canonical tag list in system prompt. (Completed in Module A implementation, June 2026. Revised from Web Worker to main-thread due to browser nested-Worker limitation — see TDD §3.3.)
4. **Onboarding UI** `[Status: Implemented]` — `/dashboard/profile-management` page with 3-presentation state machine (CV upload → onboarding review → profile management), React Hook Form + Server Actions via `useActionState` + `startTransition`, 5-Major-Skills drag-and-drop constraint, inline validation errors, sonner toast notifications, persona save + embedding generation. (Completed in Module A implementation, June 2026.)
5. **Inngest Orchestration Base** `[Status: Implemented]` — `app/api/inngest/route.ts` serve handler (Next.js App Router, `maxDuration: 300`), typed Inngest v4 client (`src/inngest/client.ts` with `VectorMatchEvents` catalog including Module C events `match/gate-3-evaluate`, `match/approved`, `match/bulk-reprocess`, and `persona/updated`), function registry (`src/inngest/functions.ts` with 65 functions — `jobIngestedHandler` for Module C normalization + Gate 0.5 pre-filter + Gate 1+2, `gate3Evaluator` for Module C LLM arbitration with A/B test prompt variants + work authorization directives + 30s timeout + 8K char input truncation, `pendingQueueSweep` for stuck pending rows, `personaUpdatedHandler` for Gate 3 feedback loop, `batchPollTier` for G5 batch polling, `normalizationRetrySweep` for stuck-job recovery (limit 500/run), `pipelineHealthMonitor` for real-time pipeline alerting, `matchBulkReprocess` for retroactive matching of existing jobs, `matchRetrySweep` for daily re-matching of missed jobs, `emergencyStoragePurge` for tiered emergency storage purge with WAL inflation protection, `hourlyStorageMonitor` for Neon synthetic storage monitoring, `v2FrontendJobScanner` for P2-2 frontend job board scanning via Brave Search, `v2GithubEventsProbe` for P1-2 GitHub Events API polling of YC/VC-funded orgs, `v2FundingSignalRss` for v2 RSS/Atom funding feed sourcing, `directJobBoardIngestion` for direct ingestion from 6 remote-first job boards (Himalayas, NoFluffJobs, RemoteOK, Arbeitnow, Remotive, WeWorkRemotely), `pollBacklogSweeper` for polling never-polled companies from the 10,114-company registry, plus 44 Module B/A functions), barrel exports (`src/inngest/index.ts`), dev server scripts (`npm run inngest:dev`), MCP integration in `.devin/config.json`, and coding agent resources (`docs/reports/inngest-agent-resources.md`). (Completed June 2026, Gate 3 feedback loop + pending sweep added June 28 2026, pipeline health monitor added July 1 2026, bulk reprocess + retry sweep added July 1 2026, Gate 0.5 pre-filter + storage monitoring + emergency purge added July 4 2026, work authorization + Neon API integration + WAL inflation protection added July 4 2026, v2 frontend-targeted sources added July 7 2026, direct job board ingestion + backlog sweeper added July 2026.)
6. **Module B Schema Layer** `[Status: Implemented]` — `company` table (ATS slug registry with tier/health/polling state, unique index on `(atsSource, atsSlug)`), `ingestionLog` table (observability), `job` table updates (`externalJobId`, `lastSeenAt`, `status`, unique constraint for dedup). Migration `0008_module_b_ingestion_tables.sql` applied. (Completed June 2026.)
7. **Module B Contracts** `[Status: Implemented]` — ATS endpoint registry (`src/lib/jobs/ats-endpoints.ts`), defensive Zod schemas for Greenhouse/Lever/Ashby (`src/lib/jobs/ats-schemas.ts`), Gate 0 regex title filter (`src/lib/jobs/gate-zero.ts`), seeder schemas (`src/lib/jobs/seeders/schemas.ts`), URL parser (`src/lib/jobs/seeders/url-parser.ts`), company repository (`src/lib/jobs/seeders/company-repository.ts`), HN Algolia schemas (`src/lib/jobs/seeders/hn-schemas.ts`). All with comprehensive Vitest test coverage. (Completed June 2026.)
8. **Seeders** `[Status: Implemented — Corpus Expansion + Corpus-Persona Alignment Complete]` — HN Algolia delta seeder domain logic fully implemented (`src/lib/jobs/seeders/hn-algolia.ts`, `resolve-custom-url.ts`) with Zod validation, CNAME resolution, slug probe, and company insertion. Inngest function wrappers registered (`hnAlgoliaSeeder`, `customUrlResolver` in `src/inngest/functions.ts`) with weekly cron trigger and event-driven custom-URL resolution. BigQuery volume seeder implemented (`src/lib/jobs/seeders/bigquery-seeder.ts`) with injectable BQ client, optimized SQL query using Wappalyzer `technologies` column (270x cheaper than payload scan), `ats_source` hint for targeted slug probing, and `GOOGLE_APPLICATION_CREDENTIALS_B64` support for Coolify. Manual script wrapper (`scripts/seed-bigquery.ts`) with `dotenv.config()` for local execution. Inngest function `bigQuerySeeder` wired up with monthly cron trigger. crt.sh batch seeder (B8) implemented (`src/lib/jobs/seeders/batch-sources/crt-sh.ts`) replacing disabled Rapid7 FDNS. All with comprehensive Vitest test coverage. (Completed June 2026, BigQuery query optimized June 25 2026, crt.sh added Sprint 4 June 30 2026, dotenv fix July 7 2026.)

  **Corpus Expansion Pipeline (June 29 2026):** 10 batch sources (B1-B10) + 13 daily-native sources (D1-D13) implemented and wired into Inngest with staggered cron triggers. Batch flush executed: 449 → 5,290 companies (106% of 5,000 target). Sources: B1 Workable Meta-Search, B3 YC Directory, B4 VC Portfolios, B5 Newsletter Archives, B7 Wayback CDX (4,163 companies — top source), B9 Cross-Pollination, B10 Sitemap Probe. Daily sources: D2 HN Algolia, D3 Reddit RSS, D4 Remote Job Boards, D5 WWR RSS, D6 CertStream, D7 Funding Signal, D8 Product Hunt, D9 Engineering Blogs, D10 GitHub Trending, D11 Tech News RSS, D12 NPM Registry, D13 Meta Ads. Two sources disabled: D1/B2 Google CSE (API discontinued — revisit with Brave Search API), B8 Rapid7 FDNS (commercial licensing — D6 CertStream covers same approach). Workable API schema drift fixed (June 2026: `company.name`→`company.title`, slug extracted from `company.url`). Slugger retry queue + `canonical_name` column + `active_hot` tier + `normalized_text` column added (migrations 0016-0028). G7 backfill: 4,491 jobs processed, ~31MB reclaimed. See `docs/reports/CORPUS_EXPANSION_TDD.md` (implementation TDD) and `docs/reports/CORPUS_EXPANSION_HANDOFF.md` (session handoff + post-flush results). 2,640 Vitest tests total (119 test files) as of July 2026.

  **Sprint 2 — Quality Architecture (June 30 2026):** Five quality features implemented and wired into Inngest, completing the quality architecture from CORPUS_EXPANSION_TDD §3. **G1 (Adaptive Polling Cadence):** `recalculateTiers()` now promotes companies to `active_hot` when they have approved matches in the last 30 days (EXISTS subquery joining match_queue → job → company on ats_source + ats_slug). Tier order: dead → active_hot (approved matches) → active_hot (Q4 bootstrap <48h) → active (jobs in 14d) → dormant. **Q4 (Bootstrap Polling):** `company.tier` default changed from `"dormant"` to `"active_hot"` — new companies get polled every 3h for the first 48h, then demote based on job count. Migration 0029. **Q2 (Adversarial Quality Flywheel):** New `company_quality_score` table tracks per-company approval rates. Daily `qualityFlywheelRecalc` Inngest function (cron `30 4 * * *` — staggered 30 min after tierRecalc) aggregates match_queue data, upserts scores, promotes high-quality companies (score > 50 AND approved > 3 → active_hot), demotes low-quality (score < 10 AND total > 20 → dormant, respects Q4 48h protection), and flags purge candidates (0 approved in 90d). Migration 0030. **Q3 (Layoff Signal Checker):** Daily `layoffSignalChecker` Inngest function (cron `0 5 * * *`) fetches Layoffs.fyi RSS feed, parses company names (handles CDATA, HTML entities, suffix stripping), matches against company_name + canonical_name via ILIKE, demotes from active_hot → active. **Q5 (Multi-Intent Fusion Scoring):** New `fusion_score` column on company (default 1) + `company_discovery_sources` tracking table with unique(company_id, discovery_source). Slugger calls `recordDiscoverySource()` after resolution — increments fusion_score when a NEW source discovers an existing company. Migration 0031. 52 new Vitest tests (1,376 total, 65 test files). See TDD §4.7 for full implementation details.

  **Sprint 3 — Production Hardening (June 30 2026):** Ten hardening tasks implemented addressing production stability and reliability issues identified during post-Sprint-2 analysis. **G8 (Aggressive Job Cleanup):** Daily `aggressiveCleanup` Inngest function (cron `0 2 * * *`) deletes terminal-state jobs (rejected >1d, gone >7d, normalization_failed >7d), archives old match_queue rows (approved/rejected >90d), deletes old ingestion_log entries (>30d) and exhausted slugger_retry entries (retry_count ≥3, >30d past). Weekly `vacuumAnalyze` function (cron `0 2 * * 0`) reclaims dead tuple space. **Gate 2 Threshold:** `GATE2_MAX_COSINE_DISTANCE` made env-configurable (default lowered 0.48→0.50) — tunable without redeploy. **B1 Workable Slug Fix:** Fast-path slug validation via HEAD request to Workable widget API; invalid slugs route through Slugger with `insertCompany: true`. **Circuit Breakers:** New `source_health` table (migration 0032) tracks per-source consecutive failures, success/failure timestamps, and disabled status. All 22 source Inngest functions wrapped with check-health/record-success/record-failure steps. Auto-degrades at 3 consecutive failures, hard shutdown at 5. **Batch Source Refresh Crons:** B1 (monthly), B3/B7 (quarterly), B4/B5/B9 (monthly), B10 (weekly) — batch sources now refresh periodically, not just flush-once. **Slugger Retry Processor:** Weekly Inngest function (cron `0 0 * * 1`) retries failed Slugger resolutions with exponential backoff (7d→14d→28d), max 3 retries. **Brave Search API:** Google CSE replaced with Brave Search API (D1/B2 revived). `brave-search.ts` seeder created, old Google CSE functions removed from route registration, `google-cse.ts` marked deprecated. **Cron Staggering:** `qualityFlywheelRecalc` moved from 04:00 to 04:30 UTC to avoid race condition with `tierRecalc`. **pendingQueueSweep:** Reduced from every 15 min (2,880 runs/mo) to every 30 min (1,440 runs/mo) — halves Inngest execution cost. **Q5 Fusion Score Backfill:** `insertDiscoveredCompanies` now calls `recordDiscoverySource()` for future direct-insert companies; backfill script created for existing ~4,163 Wayback CDX companies. 65 new Vitest tests (1,441 total, 70 test files), 0 TS errors, 1 new migration (0032). See `docs/reports/CORPUS_EXPANSION_HANDOFF.md` Sprint 3 section for full details.
  **Sprint 4 — Hardening + Observability + Admin Dashboard (June 30 2026):** Eight tasks implemented across two sub-sessions (Sprint 4 + Sprint 4b). **SmartRecruiters Tier 1 Enrichment:** `extractJobContent` for SmartRecruiters now synthesizes a pseudo-description from metadata fields (title + department + employment type + location + company name) instead of title-only, giving embeddings more semantic surface area at zero API cost. **crt.sh Batch Seeder (B8):** New `crt-sh.ts` seeder queries Certificate Transparency logs via `crt.sh/?q=%25.boards.greenhouse.io&output=json` for historical ATS domain discoveries. Replaces disabled Rapid7 FDNS. Monthly refresh cron. **VC Portfolio Expansion:** B4 expanded from 53 to 76 VC funds (European, APAC, niche/vertical VCs added). **Newsletter Expansion:** B5 expanded from 5 to 14 newsletters (Python, Go, Ruby, Postgres, iOS, DevOps, Kubernetes, Android, TLDR added). **Pre-Flight Storage Check:** `storage-check.ts` module checks Neon storage before batch refresh runs — skips refresh and logs warning if storage > 450MB (88% of 512MB limit). Integrated into all 9 batch source Inngest functions. **Admin Dashboard — Infrastructure Health:** `InfrastructureHealth` Server Component shows Neon storage usage, Gate 2 threshold, and source health table with circuit breaker status. **Admin Dashboard — Matching Funnel:** `MatchingFunnel` Server Component shows funnel analysis (jobs → Gate 0 → Gate 1+2 → Gate 3 → approved), tier distribution, quality score distribution, fusion score distribution, top companies, and purge candidates. **SmartRecruiters Tier 2 Selective Detail Fetch:** `smartrecruiters-detail.ts` fetches the detail endpoint only for jobs where Tier 1 pseudo-description is < 100 chars, capped at 10 fetches per poll cycle. Runs after Gate 0 filtering to avoid wasting API calls on rejected jobs. **Alerting System:** `alerts` table (migration 0033) with `alert_type` and `alert_severity` enums. `alerting.ts` module with `checkStorageAlerts`, `checkSchemaValidationAlerts`, `createCircuitBreakerAlert` functions with deduplication. `dailyHealthCheck` Inngest function (cron `0 6 * * *`) runs storage + schema validation checks. Circuit breaker integration auto-creates alerts on source disable. **Admin Interactivity (Sprint 4b):** `src/actions/admin.ts` Server Actions (`disableSourceAction`, `enableSourceAction`, `resolveAlertAction`, `resolveAlertsByTypeAction`) with `requireRole("admin")` auth checks, Zod validation, and `revalidatePath`. `AlertResolveButton` and `SourceToggleButton` client components using `useTransition`. Admin sidebar navigation fixed — "Admin" now links to `/dashboard/admin` with Dashboard + Users sub-items. 123 new Vitest tests (1,564 total, 78 test files), 0 TS errors, 1 new migration (0033). See `docs/reports/CORPUS_EXPANSION_HANDOFF.md` Sprint 4 + Sprint 4b sections for full details.
  **Sprint 5 — Inngest Self-Hosting Migration (June 30 2026):** Migrated Inngest operations from Inngest Cloud (free plan: 5 concurrent steps, 50K executions/month) to self-hosted Inngest on the existing Hetzner/Coolify infrastructure. Deployed a 3-container Docker Compose service (`inngest/inngest:v1.34.0` + `postgres:17` + `redis:7`) via Coolify REST API as a new service (UUID `otrzmmwzdh8z6hcg5at9yi03`) in the VectorMatch project, accessible at `https://inngest.vectormatch.dev` through Cloudflare → Traefik. Generated secure keys with `openssl rand -hex 32` (signing key, event key, Postgres password). Updated VectorMatch app env vars (`INNGEST_BASE_URL`, `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY`, `INNGEST_SERVE_ORIGIN`) via Coolify REST API bulk upsert and redeployed — `instrumentation.ts` auto-sync registered all 45 functions with the self-hosted server. Verified end-to-end: app registered (functionCount=45, SDK v4.8.0, framework nextjs), test event `poller/run` triggered function run `01KWD5GYJ4RCS6N0FTCTC6M8JY` → status `Completed` in 433ms, all 40 cron + 16 event triggers visible in dashboard. Optional code change: `src/instrumentation.ts` fallback improved to use `NEXT_PUBLIC_SITE_URL` as secondary fallback before `http://localhost:3000`. 1,584 tests pass (83 files), 0 TS errors in changed file, 0 new migrations. Inngest Cloud project kept active for 48h rollback window. See `docs/reports/CORPUS_EXPANSION_HANDOFF.md` Sprint 5 completion report for full details.

  **Sprint 7 — Pipeline Activation & Monitoring (July 1 2026):** Eight tasks implemented to fix a critical production issue (2,006 new jobs detected but not normalized/embedded, zero new matches for 32 hours) and establish comprehensive pipeline monitoring. Root cause: the `batchPollTier` function's monolithic `poll-batch` step polled 100 companies sequentially with no fetch timeout — one hanging ATS endpoint stalled the entire batch, and Inngest retries reset progress to zero. **Task 1+7+3 (CRITICAL — batchPollTier refactor):** Removed inline normalization from `batchPollTier`. The function now queries the DB for unnormalized jobs from polled companies and emits `job/ingested` events, delegating normalization to the robust `jobIngestedHandler`. The monolithic `poll-batch` step is chunked into `POLL_CHUNK_SIZE` (10) sub-batches, each its own `step.run()` — Inngest checkpoints each completed chunk so progress accumulates monotonically across retries. Writes `batch_poll` entries to `ingestion_log` for observability. **Task 2 (CRITICAL — normalizationRetrySweep):** Now processes `active` jobs with `normalized_at IS NULL` (never normalized) in addition to `normalization_failed` jobs — catches the batchPollTier timeout/retry failure mode. Limit raised from 50 to 200 jobs per run. **Task 4 (HIGH — source_health UPSERT):** `recordSourceSuccess` and `recordSourceFailure` in `source-health.ts` converted from UPDATE-only to UPSERT (INSERT ... ON CONFLICT DO UPDATE). The previous code silently affected 0 rows on first run (no row existed yet), leaving `source_health` permanently empty. **Task 5 (HIGH — error logging):** `jobIngestedHandler` now logs normalization failures with `console.error` (atsSource, jobId, error message) — previously silently swallowed, making it impossible to diagnose OpenAI API key issues or rate limiting. **Task 6 (HIGH — pipeline health monitor):** New `pipelineHealthMonitor` Inngest function (cron every 30 min) collects 8 pipeline metrics (unnormalized jobs, unembedded jobs, companies polled in 4h, matches in 24h, source_health rows, DB size, stale pending matches, normalization_failed count), evaluates against thresholds, and creates/resolves `pipeline_health` alerts (deduplicated). New `PipelineHealthMonitor.tsx` admin dashboard Server Component displays real-time metrics with color-coded status indicators. New `pipeline-health.ts` module with `getPipelineHealthMetrics()` and `evaluateAlerts()`. **HTTP Fetch Timeout:** New `fetch-with-timeout.ts` utility wraps ATS fetch calls with a 10s `AbortController` timeout — prevents indefinite hangs on unresponsive ATS endpoints. Integrated into `ats-adapters.ts` (jobs-list fetch) and `smartrecruiters-detail.ts` (detail fetch). **Migration 0034:** Adds `batch_poll` to `ingestion_log_type` enum and `pipeline_health` to `alert_type` enum. 1,594 tests pass (84 files), 0 TS errors, 1 new migration (0034). See TDD §4.7.10 for full implementation details.
  **Sprint 8 — Match Delivery Fix & Bulk Reprocessing (July 1 2026):** Ten tasks implemented to fix the broken matching pipeline — despite 9,637 companies and 9,126 active jobs ingested, only 62 matches existed (1 approved, 61 rejected). Root cause: 6 distinct issues (no bulk reprocessing, 4,083 unembedded jobs, aggressive cross-posting dedup, conservative workplace pre-filter, low Gate 3 approval rate, no periodic re-matching). **Task 1 (Bulk Reprocessing):** New `matchBulkReprocess` Inngest function (event-triggered via `match/bulk-reprocess`, concurrency limit 1). Queries active+embedded jobs NOT in match_queue (LIMIT 1000), processes in batches of 25 with parallel `runGateSQLRouter` calls (`Promise.all`), fans out Gate 3 events per batch. Admin dashboard "Run Bulk Reprocess" button (`BulkReprocessButton.tsx`) triggers via Server Action. **Task 2a (Normalization Retry Limit):** `normalizationRetrySweep` limit raised from 200 to 500 jobs/run to clear the 4,083-job backlog faster. **Task 3 (Cross-Posting Dedup Relaxed):** Gate 1+2 dedup clause now blocks only `approved` matches (not rejected/pending) — allows re-evaluation of jobs previously rejected for a different persona. **Task 4 (Workplace Pre-Filter Removed):** Removed `workplace_type` vs. `assignment_types` pre-filter from Gate 1+2 — lets Gate 3 (LLM) make the final determination, especially for hybrid roles. **Task 5 (Gate 3 Prompt Tuned):** Updated all 3 prompt variants with international contractor guidance (W-8BEN compliance), hybrid as soft concern (not auto-reject), and balanced approach (not overly conservative). **Task 6 (Periodic Re-Matching Sweep):** New `matchRetrySweep` Inngest function (cron daily 07:00 UTC) catches jobs missed by normal pipeline — queries active+embedded jobs older than 1h with no match_queue entry, processes in batches of 25 with parallel Gate 1+2. **Task 7 (Match Monitoring Metrics):** Added 4 new metrics to `pipeline-health.ts` (approvedMatches24h, gate3ApprovalRate7d, unmatchedEmbeddedJobs, avgGate3Confidence) and updated `PipelineHealthMonitor.tsx` with new alert thresholds. **Task 8 (Verification Tests):** New tests for Gate 1+2 (relaxed dedup, no workplace filter), Gate 3 (prompt tuning), pipeline health (new metrics), and `parseVectorString` utility. **Task 9 (personaUpdatedHandler Improved):** Now triggers bulk reprocess for new jobs (not just re-evaluating rejected ones) — emits `match/bulk-reprocess` event after persona update. **Task 10 (Rejection Pattern Analysis):** New `admin-queries.ts` queries for Gate 3 rejection patterns by prompt variant, persona, and ATS source. New `RejectionPatternAnalysis.tsx` admin dashboard component with approval rate tables. **SQL Fixes (post-deploy):** Fixed `SELECT DISTINCT ... ORDER BY` incompatibility (removed unnecessary DISTINCT from bulk reprocess query, added `detected_at` to select list for retry sweep). Fixed `cannot cast type record to uuid[]` by using raw SQL array literal instead of Drizzle parameterized array. **Performance Fix:** Parallelized Gate 1+2 calls within batches using `Promise.all` — reduced bulk reprocess runtime from ~41 min to ~3-5 min. 1,623 tests pass (86 files), 0 TS errors, 0 new migrations. See `docs/reports/CORPUS_EXPANSION_HANDOFF.md` Sprint 8 section for full details.
  **Sprint 9 — Storage Monitoring, Gate 0.5, Work Authorization & WAL Protection (July 4 2026):** Six critical changes implemented across storage monitoring, matching pipeline, and emergency purge. **Gate 0.5 Geo-Fencing Pre-Filter:** New hard-blocker pre-filter (`src/lib/jobs/gate-zero-pre-filter.ts`) between normalization and Gate 1+2. Catches three geo-fencing patterns: title region tags (e.g., "Software Engineer - Latam"), location country lists, and default on-site assumption for jobs without remote designation. Five checks (title region, location country list, default on-site, compensation mismatch, experience gap) — checks 4-5 are soft-fail-open. Migration 0039 adds 8 job columns + 2 applicant columns. Full design in `docs/reports/GATE_0_5_GEO_FENCING_HANDOFF.md`. **Work Authorization Filtering + Risk Flagging:** New `applicant.workAuthorizations` (text array: eu_citizen, rwr_card_plus, blue_card_eu, uk_settled, uk_pre_settled, us_green_card, us_citizen, canadian_pr, swiss_permit_c, other_permit) and `matchQueue.workAuthRiskFlag` (boolean). Gate 3 LLM checks job work-auth requirements against applicant's permits (hard blocker if missing). `workAuthRiskFlag` set when JD is silent on work auth but role is hybrid/single-country-remote. Dynamic work authorization directive injected into prompt. Migration 0040. **Neon API Integration for Storage Monitoring:** New `src/lib/jobs/neon-api.ts` fetches `synthetic_storage_size` from Neon API (the enforced limit, ~12% larger than `pg_database_size`). Dual-threshold strategy: hot-path ingestion guard uses `pg_database_size` with `STORAGE_LIMIT_MB = 460` (safety margin), hourly monitor uses Neon API with true 512 MB limit. Env vars: `NEON_API_KEY`, `NEON_PROJECT_ID`. **Simplified Storage Alerting:** Removed unnormalized backlog checks and email notifications from `checkStorageAlerts()` — alerts now create database records only, focused solely on storage percentage. Backlog monitoring handled by separate `pipelineHealthMonitor`. **WAL Inflation Protection in Emergency Purge:** `runEmergencyPurge` now tracks storage before/after each batch. If storage increases for 2 consecutive checks (DELETE generating more WAL than it reclaims), the purge aborts immediately. `active_fifo` tier batch size reduced from 1000 to 500. Recovery threshold uses `STORAGE_LIMIT_MB` (460) instead of hardcoded 512. New `PURGE_MAX_WAL_INFLATION_BATCHES = 2` and `PURGE_ACTIVE_FIFO_BATCH_SIZE = 500` constants. **Per-batch VACUUM fix:** VACUUM ANALYZE now runs after each batch (not just between tiers) so `pg_database_size()` reflects reclaimed space at the recovery check — fixes a bug where the purge reported "not recovered" even when storage dropped to 35%. **Active FIFO corpus protection:** Three safeguards prevent corpus wipe — 48h age protection, normalized-only filter (excludes jobs still in normalizer queue), and 20% corpus percentage guard (aborts if active_fifo would delete more than 20% of active jobs in one run). 1,761 tests pass (90 files), 0 TS errors, 2 new migrations (0039, 0040).
  **Sprint 10 — Corpus-Persona Alignment (July 7 2026):** Thirteen tasks implemented to align the job corpus with the mission of serving frontend/web developers seeking remote startup roles. Company registry grew from 5,290 to 10,114 companies. **P0-1 (Company Scorer Backfill):** New `src/lib/jobs/company-scorer.ts` computes `company_size_score` from 5 signals (employee count, agency flag, public listing, source origin, maturity[disabled]) clamped to [-0.30, +0.30]. `src/lib/jobs/company-enrichment/big-tech-registry.ts` provides employee count + public listing fallbacks for 30+ big-tech companies. 4 scorer bugs fixed (maturity disabled, atsSlug fallback, applyCompanyTier demotion, defense companies added). Backfill applied: 932 → `active_hot`, 9,126 → `active`, 36 → `dormant`, 2 → `dead`. 10 defense companies demoted. **P1-1 (Employee Count Backfill):** New `scripts/backfill-employee-count.ts` — YC-sourced companies set `emp=30`, VC-sourced set `emp=100`. 1,431 companies updated. Applied before P0-1 to ensure correct scoring. **P0-3 (Remote-Scope Backfill):** New `scripts/backfill-remote-scope.ts` uses v2 `src/lib/jobs/remote-scope-extractor.ts` (2-step ladder: Step 1 deterministic regex + ATS-native workplaceType + Cheerio content extraction, Step 2 LLM gpt-4o-mini fallback). 3,217 jobs classified, ~$0.71 LLM cost. **P1-2 (GitHub Events Probe):** Expanded `YC_VC_FUNDED_ORGS` from 53 to 129 frontend-ecosystem orgs in `src/lib/jobs/seeders/daily-sources/github-events-probe.ts`. Requires `GITHUB_TOKEN`. **P1-3 (Brave Search Frontend Queries):** Added 6 frontend-targeted search queries to `src/lib/jobs/seeders/batch-sources/brave-search.ts` (React, Next.js, Vue, Angular, Svelte, TypeScript job postings on ATS domains). Requires `BRAVE_SEARCH_API_KEY`. **P2-2 (Frontend Job Scanner):** New daily source `src/lib/jobs/seeders/daily-sources/frontend-job-scanner.ts` scans Greenhouse/Lever/Ashby job boards for frontend-keyword postings via Brave Search, extracts company slugs from URL patterns. Registered as `v2FrontendJobScanner` Inngest function. **P2-1 (BigQuery Re-run):** 3,778 domains found, 448 slugs resolved, 0 new companies (corpus saturated from monthly runs). **P0-4 (Backlog Normalization):** `scripts/direct-normalize-backlog.ts` supports `--dry-run`/`--apply`. Applied: 1,237 jobs processed (1,104 normalized, 109 rejected, 24 failed on >8192 token limit). **Circuit Breaker:** 5-tier action chain (`src/lib/jobs/circuit-breaker.ts`) — Tier 1 per-source early-warning (3 fails → pause), Tier 2 provisional backlog throttle (>15% → rate reduce, >30% → pause), Tier 3 unknown sub-floor guard (≥30% unknown → force reclassify), Tier 4 corpus-ratio breaker (global <50% → halt non-global), Tier 5 daily source ban (3 escalations → 24h ban). **Migrations:** 0041-0045 (employee_count, company_size_score, is_agency, is_public, remote_scope columns + `frontend_job_scanner` enum). 2,260 tests pass (110 files), 0 TS errors, 0 Biome errors. See `docs/reports/CORPUS_ALIGNMENT_SESSION_HANDOFF.md` for the full session report.
  **Sprint 11 — Ingestion Freshness & Retention Governance (July 2026):** Post-alignment cleanup to keep the corpus current and storage-sustainable. **Injection Freshness Gate:** 30-day hard cap in `phalanx-poller.ts` rejects jobs whose `publishedAt` is older than 30 days; `NULL` `publishedAt` is also rejected. **Active/Closed Filtering:** `job-normalizer.ts` metadata includes `isActive`; explicit-status sources (SmartRecruiters, Recruitee) reject closed/archived/filled postings, while Greenhouse/Lever/Ashby/Workable are assumed active. Detail enrichment for SmartRecruiters now drops closed jobs from the detail endpoint. **Age-Based Stale Marking:** `MAX_JOB_AGE_DAYS` set to 60; daily `staleCleanup` marks active jobs older than 60 days as `stale`. Backfill script `scripts/purge-stale-jobs-by-age.ts` marked 1,741 jobs stale. **90-Day Hard Delete:** New `scripts/hard-delete-ancient-jobs.ts` (dry-run by default) + `deleteAncientJobs()` helper called by `aggressiveCleanup` Inngest function (daily 02:00 UTC) removes jobs older than 90 days; first run deleted 2,676 rows. **Company Count Backfill:** `company-state.ts` gained `backfillCompanyActiveJobCounts()` and `scripts/backfill-active-job-counts.ts` to fix `activeJobCount` after purges. **Admin Analytics:** New `JobStalenessDistribution` card with `StalenessDistributionChart`, `OldJobRateAlertPanel` for high old-job-rate sources, and freshness badges on public `JobCard` listings. **Hydration Fix:** Extracted Radix Tooltip from `InfrastructureHealth` Server Component into a dedicated `"use client"` `NeonStorageTooltip` component to avoid React hydration mismatch. No new migrations. Build, TypeScript, Biome, and targeted Vitest tests pass.
  **Sprint 12 — Gate Hardening & LLM Safeguards (July 2026):** Targeted fixes to Gate 0.5 and Gate 3 addressing false positives discovered in production where hybrid and null-workplaceType jobs in foreign countries were passing through to Gate 3. **Gate 0.5 hybrid hard-block:** `gate-zero-pre-filter.ts` now hard-blocks hybrid jobs when `remoteScope != 'global'` and the location doesn't match the applicant's country — previously hybrid was treated as soft concern, letting foreign-country hybrid jobs through. **Gate 0.5 null workplaceType hard-block:** Jobs with null `workplaceType` and specific city locations (e.g., "Pune, MH, in") are now blocked when the location has no remote indicators and doesn't match the applicant's country — previously these passed through as "maybe remote." **Gate 3 prompt strengthening:** All 3 prompt variants updated — (1) null `workplaceType` with a specific city location is now inferred as on-site (not "maybe remote"), (2) hybrid in a foreign country is a hard blocker (not soft) unless in the applicant's country/region, (3) full-time/W-2 roles treat work authorization as a hard blocker while contractor/B2B roles treat it as advisory only (not a blocker). **Gate 3 LLM timeout:** All Gate 3 LLM calls now use `AbortSignal.timeout(30000)` (30 seconds) to prevent indefinite hangs on unresponsive OpenAI API responses. **Gate 3 input truncation:** Job description input to the LLM is truncated to 8,000 characters to prevent token-limit failures on oversized postings (previously 24 jobs failed on >8192 token limit). **Resurrection sweep fix:** `nightlyResurrectionSweep` Inngest function fixed — step output was exceeding Inngest's 256KB limit when processing large batches; now chunks results to stay within limits. 5 new tests in `gate-zero-pre-filter.test.ts` covering hybrid foreign country and null workplaceType patterns. See TDD §4.7.15 for full implementation details.
  **Sprint 13 — Direct Job Board Ingestion & Backlog Sweep (July 2026):** A parallel ingestion path that bypasses the ATS poller entirely, fetching jobs directly from remote-first job boards with structured APIs. This addresses the corpus skew (337 backend jobs vs 98 frontend jobs for target personas) and the polling bottleneck (6,516 of 10,114 companies never polled due to 45-day full-cycle throughput). **Direct Ingestion Pipeline:** New `src/lib/jobs/direct-ingestion/` module with 6 board adapters — Himalayas (103K+ jobs, paginated GET), NoFluffJobs (11,446 jobs, 9,320 remote — CEE/EU focus, single 80MB GET response, `location.fullyRemote` filter, monthly→annual salary ×12, seniority→years mapping), RemoteOK (single GET, skips legal notice), Arbeitnow (EU-focused, `?page=N` pagination with empty-page detection), Remotive (remote-first, country-fenced scope detection), WeWorkRemotely (RSS XML feed, regex parsing, no XML-parser dependency). Each adapter transforms board-specific data into a unified `DirectIngestionJob` shape, applies a persona tech-stack filter (`filter.ts` with 50+ frontend/PHP/Laravel keywords and word-boundary matching), and upserts directly into the `job` table with structured fields + embeddings + `normalizedAt` set — skipping Gate 0, normalization, and Gate 0.5 entirely. **`directJobBoardIngestion` Inngest function:** Cron `0 5 * * *`, concurrency 1, orchestrates all 6 boards in sequence with per-board fetch + upsert steps. **Backlog Sweeper:** New `pollBacklogSweeper` Inngest function addresses the 6,516 never-polled companies by polling 100 never-polled companies per run (via `getNeverPolledBatch` in `tier-queries.ts`). **Profile UI for compensation & experience:** `applicant.expectedCompMin` and `applicant.yearsOfExperience` fields added to onboarding (`ApplicantSection.tsx`) and profile management (`ProfilePreferencesForm.tsx`) with Zod validation (non-negative, integer years, max $1M comp). Wired into `onboardingAction` and `profileUpdateAction`. **Evaluation script:** `scripts/evaluate-job-boards.ts` tests all 6 board APIs and reports job counts + frontend/PHP breakdowns. 60 direct-ingestion tests pass, 2,346 total tests pass (111 files), 0 TS errors, 0 Biome errors. No new migrations (uses existing `job` table columns). See `docs/system/direct-ingestion-compensation-handoff.md` for the full implementation handoff.
  **Sprint 14 — Mismatch Analysis & 6-Fix Cascade (July 2026):** Comprehensive analysis of 50 user-marked "mismatch" records from `match_queue` revealed a 5-root-cause cascade where every mismatch was initially approved by Gate 3. The fixes were applied in two stages. **Stage 1 (code changes, deployed):** (1) **Fix 1 — Empty embedding returns `[]`:** The `runGate1Only` fallback in `gate-1-2.ts` was letting unembedded jobs (probation companies + intentionally-fenced jobs) bypass Gate 2 entirely, matching on 1-tag overlap alone — 44 of 50 mismatches (88%). Fixed by returning `[]` for empty embeddings; jobs wait for the embedding backfill. (2) **Fix 2 — `GATE1_MIN_OVERLAP = 2`:** Added minimum overlap threshold to the Gate 1+2 SQL query (`ov.overlap_score >= 2`), env-configurable via `GATE1_MIN_OVERLAP`. 16 of 50 mismatches (32%) had `overlap_score = 1`. **Stage 2 (code changes, deployed):** (3) **Fix 3 — Compliance directive restructured:** The Gate 3 compliance directive was restructured from a single "OVERRIDE — DEFAULT TO APPROVING" block into a 2-step check: Step 1 hard-blocks country-fenced jobs whose list doesn't include the US (w8ben only covers US); Step 2 applies the "default to approving" rule only for US-included restrictions. 15 of 50 mismatches (30%) were non-US country-fenced jobs (CA, PL, PT, MX, IN, CL, CR) approved because the LLM applied the w8ben rule to ALL countries. (4) **Fix 4 — Management/PM role detection:** Added criterion 8 to all 3 Gate 3 prompt variants — if the job title contains management keywords AND the persona's seniority levels are IC-only, it's a HARD BLOCKER. 6+ of 50 mismatches (12%) were management/PM roles approved for IC personas. (5) **Fix 5 — Global+country-list contradiction:** When `remoteScope = 'global'`, `locationCountries` is now forced to `null` in all 3 handler paths (deterministic upgrade, LLM upgrade, resurrection sweep). 2 of 50 mismatches (4%) had `remote_scope = 'global'` with `location_countries = ['US']` — a contradiction that bypassed Gate 0.5 Check 8. (6) **Fix 6 — Gate 0.5 Check 8 hard-block for non-US:** Check 8 now hard-blocks country-fenced jobs when the list doesn't include the US (pattern `country_fenced_non_us`), regardless of fencing language in the JD. Previously, the absence of fencing language caused a soft-fail-open to Gate 3. Combined: all 50 mismatches addressed, 48 of 50 at the Gate 1+2 / Gate 0.5 level (before LLM cost). 2,640 tests pass (119 files), 0 new TS errors. See TDD §5.2-5.3 for full implementation details.
9. **Ingestion Poller** `[Status: Implemented]` — Phalanx Poller core ingestion loop fully implemented: ATS adapters (`src/lib/jobs/poller/ats-adapters.ts`) with fetch + Zod validate + normalize per platform, job repository (`src/lib/jobs/poller/job-repository.ts`) with upsert + new job detection + stale cleanup, company state updater (`src/lib/jobs/poller/company-state.ts`) with health tracking + auto-disable after 3 failures + activeJobCount backfill helper, tier queries (`src/lib/jobs/poller/tier-queries.ts`) for fan-out polling, and orchestrator (`src/lib/jobs/poller/phalanx-poller.ts`) with injectable fetch + active-status filter + 30-day injection freshness gate + detail enrichment drops. Inngest functions wired up: `pollCompanyFn` (per-company fan-out target, concurrency 5 — Inngest free plan limit), `tierActiveFanOut` (every 12h), `tierDormantFanOut` (weekly), `phalanxPoller` (manual single-company), `tierRecalc` (daily), `staleCleanup` (daily, includes age-based staleness), `aggressiveCleanup` (daily, includes 90-day hard delete). Per-ATS bottleneck rate limiter (`src/lib/jobs/poller/rate-limiter.ts`, 2 req/s per platform). All with comprehensive Vitest test coverage (37 poller/detail tests). (Completed June 2026; freshness/retention layer added July 2026.)
10. **3-Gate Routing Logic** `[Status: Implemented]` — Module C (Event-Driven Routing — The 3-Gate Funnel) fully implemented across 7 features (C0–C6):
    - **C0 (Schema & Contracts)**: `matchQueue` columns hardened (`personaId`, `cosineDistance`, `llmVerdict`, `llmReasoning`, `llmModel`, `promptVariant`, `evaluatedAt`, `isRead`), unique index corrected to `(jobId, personaId)`, `job.status` extended (`active|stale|gone|rejected|normalization_failed`), `job.normalizedAt` added, `job_embedding_hnsw_idx` added, `matching-config.ts` config module, `db.ts` pooler guard + `max: 20`, Module C event types (`match/gate-3-evaluate`, `match/approved`, `persona/updated`) added to `VectorMatchEvents` catalog. `seniority_level` enum + `applicant.seniority_levels` column added (June 28 2026).
    - **C1 (Job Normalization)**: `src/lib/jobs/job-normalizer.ts` (`extractJobContent`, `scanTagsRegex`, `extractTagsLLM`, `normalizeJob`, `decideNormalizationAction`) + `src/lib/jobs/job-embedder.ts`. Wired into `jobIngestedHandler` steps 1–4 with idempotency guard. 39 Vitest tests.
    - **C5 (Seed Script)**: `scripts/seed-routing-engine.ts` generates synthetic data (5 archetypes, N personas, M jobs) with Gaussian noise + embeddings. Verified at `--scale 100` (100 personas, 500 jobs).
    - **C2 (Gate 1+2 SQL Router)**: `src/lib/jobs/gate-1-2.ts` with `runGateSQLRouter` — single-pass GIN overlap + HNSW cosine distance query with composite ordering (`overlap * w1 + (1 - distance) * w2 DESC`), `unnest`/`= ANY` overlap count (fixes `&` operator bug on `text[]`), `LIMIT 8` + `ON CONFLICT DO NOTHING`. Cross-posting dedup via `NOT EXISTS` subquery on `(ats_slug, title, persona_id)` — **relaxed (July 1 2026):** now blocks only `approved` matches, not rejected/pending, allowing re-evaluation of jobs previously rejected for a different persona. Workplace type pre-filter (added June 28 2026) — **removed (July 1 2026):** lets Gate 3 (LLM) make the final determination, especially for hybrid roles that may offer remote options. Wired into `jobIngestedHandler` step 5. 17 Vitest tests.
    - **C3 (Gate 3 LLM Arbitration)**: `src/lib/jobs/gate-3.ts` (`gate3VerdictSchema`, `buildGate3Prompt`, `evaluateGate3`, `mapVerdict`, 3 A/B test prompt variants: `balanced`/`strict`/`thorough`, `pickPromptVariant`) + `gate3Evaluator` Inngest function for LLM evaluation, verdict writing, and `match/approved` event emission. Seniority-aware matching via `Gate3Context.applicant.seniorityLevels`. Country-specific remote restriction checks. **Prompt tuning (July 1 2026):** All 3 variants updated with international contractor guidance (W-8BEN compliance context), hybrid as soft concern (not auto-reject), and balanced approach (replaced "be conservative" bias). Registered in `src/app/api/inngest/route.ts`. 31 Vitest tests.
    - **C3b (Gate 3 Feedback Loop + Bulk Reprocess + Retry Sweep)**: `pendingQueueSweep` Inngest function (cron every 30 min — picks up stuck pending rows) + `personaUpdatedHandler` Inngest function (event-driven — re-evaluates rejected matches when persona tags change, AND triggers bulk reprocess for new jobs since July 1 2026). `updatePersonasAction` emits `persona/updated` events. **`matchBulkReprocess`** (added July 1 2026): Event-triggered (`match/bulk-reprocess`), concurrency limit 1. Queries active+embedded jobs NOT in match_queue (LIMIT 1000), processes in batches of 25 with parallel `Promise.all` Gate 1+2 calls, fans out Gate 3 events per batch. Admin dashboard "Run Bulk Reprocess" button triggers via Server Action. **`matchRetrySweep`** (added July 1 2026): Cron daily 07:00 UTC. Catches jobs missed by normal pipeline — queries active+embedded jobs older than 1h with no match_queue entry, processes in batches of 25 with parallel Gate 1+2.
    - **C4 (Dashboard Query Layer)**: `src/lib/jobs/dashboard-queries.ts` (`getMatches`, `getMatchesCount`, `getApprovedMatches`, `getUnreadBadgeCount`, `getMatchDetail`) + `src/actions/matches.ts` (`markMatchRead`, `markAllMatchesRead` Server Actions). 15 Vitest tests. **Rejection Pattern Analysis (July 1 2026):** `admin-queries.ts` queries for Gate 3 rejection patterns by prompt variant, persona, and ATS source. `RejectionPatternAnalysis.tsx` admin dashboard component with approval rate tables. **Display Match Score (July 2026):** Composite 0–100 score shown in dashboard list and detail, computed in SQL with positive signals (semantic similarity 25%, tag overlap 30%, workplace 12%, location 8%, seniority 8%, company quality 17%) and negative signals (blocklist penalty 10%, coverage gap 10%, secondary domain mismatch 8%). Formula lives in `src/lib/jobs/dashboard-queries.ts`.
    - **C6 (Calibration)**: `scripts/calibrate-routing-engine.ts` runs Gate 1+2 against seed data, collects cosine distance + overlap score distributions, measures true candidate counts at different thresholds, optionally evaluates Gate 3 verdicts. `docs/reports/calibration-report.md` documents findings. **Synthetic data (June 2026):** threshold 0.35 was a no-op (embeddings cluster at 0.18–0.21), LIMIT 8 did all filtering. **Real-data calibration (June 25 2026):** `GATE2_MAX_COSINE_DISTANCE` raised from 0.35 to 0.55. Cross-posting dedup added. **Yield analysis (June 28 2026):** Threshold tightened from 0.55 to 0.48 (85% reduction in LLM calls, no loss of true positives). Top rejection reasons: location mismatch (38%), wrong tech stack (24%), seniority mismatch (18%). Persona consolidation (3→2 TypeScript + 1 PHP/Laravel). Seniority-aware matching + A/B test prompt variants + Gate 3 feedback loop implemented. Company corpus (449 companies) identified as primary bottleneck. **Sprint 8 tuning (July 1 2026):** Cross-posting dedup relaxed (blocks only approved), workplace pre-filter removed, Gate 3 prompt retuned for international contractors. Bulk reprocess + daily retry sweep added. Pipeline health monitor expanded with 4 new metrics (approvedMatches24h, gate3ApprovalRate7d, unmatchedEmbeddedJobs, avgGate3Confidence). **Display score calibration (July 2026):** Negative signals added (blocklist penalty, coverage gap, secondary domain mismatch) to better align the score with LLM rejection reasons. Empirical analysis scripts (`analyze-approved-matches.ts`, `analyze-rejected-matches.ts`, `investigate-wordpress-matching.ts`) created. Current gap: approved average 54.1/100 vs. rejected average 39.9/100. Next targeted signal: experience gap.
    - All 2,260 Vitest tests pass (110 files), typecheck clean, biome clean. (Completed June 2026, yield analysis improvements June 28 2026, Sprint 8 matching pipeline fix July 1 2026, display score + negative signals July 2026, corpus-persona alignment July 7 2026.)
11. **Module E: Infrastructure & Deployment** `[Status: Implemented — deployed and healthy, June 2026]` — Complete deployment infrastructure on Hetzner Cloud CX33 (Helsinki) + Coolify v4.1.2. 3-stage Dockerfile (Node 24-slim, standalone output, multi-arch, non-root user, curl for healthcheck). Lazy-initialization pattern applied to `src/db/db.ts` (Neon Pool via lazy Proxy) and `src/lib/email.ts` (Resend client via `getResend()`) — no runtime secrets needed at Docker build time. `output: "standalone"` in `next.config.ts`. `/api/health` endpoint for Coolify healthcheck. `.dockerignore` for clean Docker context. Inngest concurrency lowered from 50/15/15 to 5/5/5 to match free plan limit. PPR confirmed working under standalone Docker output with `cacheComponents: true`. Cloudflare WAF rate-limiting rules and Hetzner firewall configuration documented in TDD §7.6. (Completed June 25, 2026.)


## Technical Architecture

### Core Tech Stack `[Status: Partially Implemented]`
- **Framework**: Next.js 16 (App Router + Cache Components)
- **Database**: PostgreSQL (Neon) with `pgvector` extension for similarity search
- **ORM**: Drizzle ORM for schema management and type-safe querying
- **Auth**: Better Auth for secure user management and authentication
- **Orchestration**: Inngest v4 for durable, event-driven background jobs and workflows
- **AI SDK**: Vercel AI SDK (`gpt-4o` for complex reasoning, `gpt-4o-mini` for scale, `text-embedding-3-small` for vector generation)
- **Styling & UI**: Tailwind CSS v4 + Shadcn UI (using CSS-first `@theme` configuration)
- **Blog / Content**: **WordPress + custom VectorMatch PHP theme** deployed as a native Coolify service on the same Hetzner server, served at `/blog` via Traefik reverse proxy. The custom classic theme is maintained under direct file control (not Elementor) and mirrors the Next.js design system. Fully decoupled from the Next.js application — no MDX, no in-repo content, no Neon dependency. See *Blog & Content Architecture* below.
- **Hosting:** Hetzner Cloud CX33 (Helsinki, eu-central) + Coolify v4.1.2 (self-hosted PaaS). Neon Postgres in Frankfurt (aws-eu-central-1). Cloudflare edge protection. `[Status: Implemented — deployed and healthy, June 2026]` 

### Database Schema & Performance Tuning
- **`users` table** `[Status: Implemented]`: Managed by Better Auth.
  - *Drizzle Path*: `src/db/schemas/auth/user.ts`
- **`applicant` table** `[Status: Implemented]`: 1:1 extension of `users`. Primary key `user_id` is a foreign key to `user.id` with CASCADE delete.
  - *Drizzle Path*: `src/db/schemas/jobs/applicant.ts`
  - *Enum Definitions*: `src/db/schemas/jobs/enums.ts` (`assignment_type`, `modality`, `compliance`, `cv_upload_status`)
  - *Onboarding State*: `is_onboarded` (boolean), `country` (ISO 3166-1 alpha-2), `can_work_us_hours` (boolean)
  - *Work Preferences*: `assignment_types` (enum array), `modalities` (enum array), `preferred_compliance` (enum array)
  - *Gate 3 Knowledge Base `[Status: Implemented]`*: Contains `all_tags` (text array) — the global knowledge base used by the LLM arbitrator during high-fidelity candidate evaluation. Rebuilt as union of active `tagsExperience.canonical_tag` values by `recomputeTagsExperience()`.
- **`persona` table** `[Status: Implemented]`: Supports multi-persona matching. Contains `persona_label`, `embedding_summary`, `persona_embedding` (vector 1536), `must_have_tags` (text array), and `blocklist_tags` (text array).
  - *Gate 1 Optimization*: `must_have_tags` and `blocklist_tags` are indexed using a **Postgres GIN index** to support instant `&&` (overlap) query operations.
  - *Gate 2 Optimization*: Indexed using **HNSW** (`USING hnsw (persona_embedding vector_cosine_ops)`) to achieve sub-millisecond vector similarity calculation during routing.
  - *Business Rule*: To prevent abuse, users are strictly limited to a maximum of 3 personas, enforced at the API/Zod validation layer.
- **`cv_upload` table** `[Status: Implemented]`: Persists every CV upload attempt — raw extracted text (from `pdfjs-dist` in main-thread "fake worker" mode via `src/lib/onboarding/pdf-worker-client.ts`), the full LLM extraction payload (Schema 1 JSONB), and a lifecycle status (`processing`, `valid`, `invalid`, `abandoned`) that drives the onboarding state machine.
  - *Drizzle Path*: `src/db/schemas/jobs/cvUpload.ts`
- **`working_history` table** `[Status: Implemented]`: Single source of truth for the user's work history. Each row represents one employment entry extracted from a CV (by the LLM) or added manually post-onboarding. Linked to `cv_upload` via `cv_upload_id` (CASCADE delete). This table is the input to `recomputeTagsExperience()`.
  - *Drizzle Path*: `src/db/schemas/jobs/workingHistory.ts`
- **`tags_experience` table** `[Status: Implemented]`: Single source of truth for the user's skills and years of experience per skill. NOT populated by the LLM directly — computed by `recomputeTagsExperience(applicantId)` which reads `working_history`, merges overlapping date ranges per canonical tag, and upserts results here. The `active` flag is managed by `recomputeTagsExperience()`; for MVP users cannot toggle skills directly. Unique constraint on `(applicant_id, canonical_tag)` enables upsert.
  - *Drizzle Path*: `src/db/schemas/jobs/tagsExperience.ts`
- **`jobs` table** `[Status: Implemented]`: Stores job listings with `ats_slug`, `title`, `raw_json`, `extracted_tags` (text array), `job_embedding` (vector 1536), `external_job_id` (dedup), `last_seen_at` (stale tracking), `status` (`active|stale|gone|rejected|normalization_failed`), and `normalized_at` (Module C idempotency guard). Indexed with GIN on `extracted_tags`, HNSW on `job_embedding`, and B-tree on `(status, last_seen_at)`.
- **`match_queue` table** `[Status: Implemented]`: Maps `job_id` to `persona_id` (not `applicant_id` — corrected for multi-persona users) with Gate 1+2 scores (`overlap_score`, `cosine_distance`), a `status` state (pending, approved, rejected), Gate 3 LLM verdict columns (`llm_verdict`, `llm_reasoning`, `llm_confidence`, `llm_blockers`, `llm_model`, `evaluated_at`), and `is_read` for in-app notification badge. Unique index on `(job_id, persona_id)` — an applicant can match the same job via multiple personas. Cross-posting dedup at insert time: Gate 1+2 checks `NOT EXISTS` on `(ats_slug, title, persona_id)` before inserting — prevents ATS cross-posted duplicates (same job listed multiple times with different `external_job_id`) from creating duplicate matches. **Relaxed (July 1 2026):** dedup now blocks only `approved` matches (not rejected/pending), allowing re-evaluation of jobs previously rejected for a different persona. Partial index on `(applicant_id) WHERE is_read = false AND status = 'approved'` for the unread badge query. Migrations `0010` (llm_confidence) and `0011` (llm_blockers) applied.
- **`blog_categories`, `blog_tags`, `blog_posts`, `blog_post_tags`, and `blog_comments` tables** `[Status: Dropped — July 2026]`: A full relational blog and nested commenting schema that was originally implemented for an in-app blog, then briefly used for a file-based MDX blog. Both approaches have been superseded by WordPress + custom VectorMatch theme (see *Blog & Content Architecture*).
  - *Decision*: These tables, their relations, and the `src/db/schemas/blog/` directory were **fully removed** via Drizzle migration. The `src/db/schemas/index.ts` file retains a comment documenting the removal. The blog is now served by WordPress at `/blog` — no blog-related tables exist in the Neon database.

## Application Architecture & User Flow

### Public Area (Marketing & Onboarding)
**Homepage Structure** `[Status: Implemented]`
- **Navigation Bar**: Logo (homepage link), main navigation links (Home, Blog, Pricing), authentication actions (Sign Up/Sign In)
- **Hero Section**: Compelling value proposition, standard action button ("Start Your AI Job Hunt") redirecting to authentication, and visual presentation of the ATS matching concept
- **How It Works Section**: Step-by-step explanation of the CV analysis → ATS matching → application workflow
- **Pitch Section**: Benefits and differentiators compared to traditional job boards
- **Footer**: Standard links (Copyright, About, FAQ, Terms, Privacy Policy)

### Blog & Content Architecture `[Status: Implemented — theme published July 2026; Elementor transition abandoned]`
The blog is a **WordPress** installation deployed as a native Coolify service on the same Hetzner CX33 server, served at `vectormatch.dev/blog` via Traefik reverse proxy. Its purpose is organic SEO acquisition, product/ATS education, developer career guidance, and conversion of anonymous visitors into registered users. It is **fully decoupled from the Next.js application** — no MDX files, no in-repo content, no Neon reads, no shared build pipeline. The frontend design system (dark theme, Geist + PT Serif fonts, Tailwind v4 tokens) is now ported directly into a custom WordPress classic PHP theme so the blog looks identical to the Next.js app.

**Key Strategic Decision:** An Elementor-based theme transition was evaluated and **abandoned**. The team kept the custom classic PHP theme under direct file control for full design ownership, better performance, simpler version control via WPVibe, and avoidance of vendor lock-in. Elementor remains installed only for optional future isolated content blocks and is not used for header, footer, single post, archive, or listing templates.

- **Architecture**: WordPress runs in its own Docker container (`wordpress:latest`) with a companion MariaDB 11 container, both managed by Coolify as a single service ("Wordpress Blog"). The Next.js app and WordPress share the same Traefik reverse proxy — Next.js serves all routes except `/blog/*`, which Traefik routes to the WordPress container.
- **Subpath Routing**: Coolify's Traefik `stripprefix` middleware removes `/blog` before forwarding requests to the WordPress container. Three volume-persisted files handle the prefix restoration:
  - `wp-blog-defines.php` — loaded by `wp-config.php`; sets `WP_HOME`/`WP_SITEURL` to `https://vectormatch.dev/blog`, forces SSL behind the reverse proxy, and restores the stripped `/blog` prefix to `$_SERVER['REQUEST_URI']` so WordPress rewrite matching aligns with `home_url()`.
  - `wp-content/mu-plugins/00-subpath-rewrite.php` — filters `mod_rewrite_rules` to keep generated `.htaccess` root-based (Apache receives stripped paths, not `/blog`-prefixed ones).
  - `.htaccess` — root-based WordPress rewrite rules + a `mod_rewrite` block that re-adds the `/blog` prefix and forces `https` on Apache directory-slash redirects (e.g., `/wp-admin` → `/blog/wp-admin/`), which Traefik's stripprefix does not restore on `Location` headers.
- **Persistence**: All WordPress files (core, themes, plugins, uploads) live in a named Docker volume (`a1yhworj7zx3hqhuhrrrkoui_wordpress-files`). MariaDB data lives in a separate volume. Both survive Coolify redeploys. The three routing files above are also volume-persisted and survive redeploys.
- **Custom Theme**: Active theme is `vectormatch-blog` (custom classic PHP theme). It includes `header.php`, `footer.php`, `index.php`, `single.php`, `page.php`, `archive.php`, `functions.php`, `theme.css` (Tailwind v4 design tokens), and a `template-parts/` directory. The theme mirrors the Next.js navbar and footer links, loads the VectorMatch logo from the WordPress media library, and uses Tailwind v4 utility classes processed by WPVibe's browser CDN. The compiled `dist/styles.css` is auto-generated on theme publish; the browser CDN is the fallback during drafting.
- **Single Post Layout (`single.php`)**: 2:1 hero image with overlaid title; post telemetry (date · category · reading time) below the hero; `.vm-lead` subtitle extracted and centered above the body; "The short answer" callout upgraded to `<h2>` and rendered as italic prose; "What you'll learn" upgraded to `<h3>` with `0.9rem` list text; `.prose` tables forced to `1.25rem` first/last cell padding via `!important`; inline SVG diagrams scaled (`14px` description text reduced to `12–13px`) to prevent overflow.
- **Archive / Category Listing (`index.php` / `archive.php`)**: H2 linked title; 2:1 featured-image thumbnail below the title linked to the post with `transform: scale(1.2)` hover transition over `0.35s`; date · linked category · reading time meta row; excerpt; "Go to the post →" link. Category pages reuse the same card layout as the main blog page.
- **Hero Images**: Generated article hero images are uploaded into the WordPress media library and set as each post's featured image. They drive the single-post hero and the archive/category thumbnails.
- **Admin & Content Workflow**: WordPress admin at `/blog/wp-admin/`. Content is managed via the WordPress admin UI. The recommended agent workflow is WPVibe: connect the site, create a draft theme, edit files via `write_file`/`edit_file`, install plugins via `run_wp_cli`, and publish the draft. WPVibe creates a backup theme (`vectormatch-blog-wpvibe-backup`) on each publish and purges relevant caches. Manual WordPress admin access is still available for plugin setup wizards (e.g., Rank Math, Wordfence 2FA) that require UI interaction or OAuth.
- **SEO**: WordPress generates its own sitemap (Rank Math: `/blog/sitemap_index.xml`; WordPress core fallback: `/blog/wp-sitemap.xml`). The Next.js `sitemap.ts` includes the `/blog` base URL so search engines discover the WP blog. Per-post Open Graph metadata is handled by Rank Math. RSS is available at `/blog/feed/`.
- **Navbar/Footer Links**: The Next.js Navbar and Footer link to `/blog` — Traefik routes these to WordPress transparently. The blog header also links back to `/jobs`, `/#how`, `/#pitch`, and `/blog`; the footer links mirror the Next.js footer (Product, Company, Legal).
- **Plugin Stack**: **Rank Math SEO** (SEO + sitemap + schema), **LiteSpeed Cache** (page cache + asset optimization), **UpdraftPlus** (daily DB backups, 7-day retention), **Wordfence Security** (must be deactivated during automated WPVibe work; reactivated by the user after), **Elementor (free)** installed for optional future isolated content blocks but **not used for templates**. Akismet was removed.
- **Taxonomy**: Six categories and 31 tags are pre-seeded to align with VectorMatch's content strategy (ATS/hiring, job search, remote work, career growth, market intelligence, product/engineering; plus skill/ATS/platform tags).
- **Security Hardening**: Enforced in the theme (`functions.php`) and WordPress options: `DISALLOW_FILE_EDIT` defined, `users_can_register` forced to `0`, default comment/pingback status closed, `blog_public` enabled, XML-RPC disabled, REST user enumeration disabled, WordPress generator tag removed, X-Pingback header removed, and taxonomy creation restricted to administrators.
- **WPVibe Integration Notes**: WPVibe is the MCP bridge that lets Devin manage the WordPress site via `run_wp_cli`, `rest_api`, `write_file`, `edit_file`, `read_file`, and theme draft/publish operations. Because Wordfence's firewall blocks WPVibe's Application Password authentication, the agent must keep Wordfence deactivated while performing automated work. After the agent finishes, the user reactivates Wordfence and completes 2FA/login setup in wp-admin. Rank Math's setup wizard must also be completed in wp-admin to activate `/blog/sitemap_index.xml` and JSON-LD schema output.
- **Migration History**: The blog was originally implemented as a file-based MDX system (`next-mdx-remote/rsc` + `gray-matter`, articles in `src/app/(public)/blog/_posts/*.mdx`). The MDX code, `src/lib/blog/` content layer, `src/components/mdx/` component library, `@tailwindcss/typography` plugin, and all MDX dependencies (`gray-matter`, `shiki`, `remark`, `rehype`) were fully removed. Deprecated `blog_*` database tables (categories, tags, posts, post_tags, comments) were dropped via Drizzle migration. The `WORDPRESS_CONFIG_EXTRA` env var (which Coolify's variable interpolation corrupted, causing 500 errors) was removed in favor of the volume-persisted `wp-blog-defines.php`. An Elementor transition was evaluated and rejected in favor of the custom PHP theme.

### User Acquisition & Authentication Flow
**Standard Authentication Flow** `[Status: Implemented]`
- Users must authenticate before accessing any dashboard features.
- Primary conversion element is a call-to-action button linking directly to the separate `/auth` page.
- Fully supports traditional sign-up/sign-in forms with email/password authentication (as well as Google/GitHub OAuth integrations).

**Smart Dashboard Redirection Logic** `[Status: Implemented]`
- **On Sign-Up (New Users)**:
  - Once signed-up, the user must be automatically redirected to the `/dashboard/profile-management` page. This ensures a natural onboarding workflow where the user is immediately prompted to upload and parse their CV (State 1 of the 3-presentation state machine).
- **On Log-In (Existing Users)**:
  - When an existing user logs in, they should be intelligently redirected based on their profile completion:
    - **a) If the user is not onboarded** (`applicant.is_onboarded = false`): redirect them to the `/dashboard/profile-management` page to complete onboarding.
    - **b) If the user is onboarded** (`applicant.is_onboarded = true`): redirect them to the `/dashboard/jobs` page to view active matches.

### Dashboard Area (Authenticated User Workspace)
**Navigation Structure** `[Status: Implemented]`
- Collapsible sidebar with role-based navigation items
- Main navigation items: Account, Profile Management, Jobs
- Admin-only navigation: Admin panel (for user management and system oversight). *Note: there is no blog management in the dashboard — blog content is managed via the WordPress admin at `/blog/wp-admin/`, fully decoupled from the Next.js application.*
- Responsive design supporting mobile and desktop layouts

**Core Dashboard Sections**

**Account Management** `[Status: Partially Implemented]`
- Profile information and settings `[Status: Implemented]`
- Security settings: password changing is `[Status: Implemented]`. Two-Factor Authentication (2FA) is `[Status: Planned / Post-Traction]` (to be implemented only once/if the application gets user traction).
- Subscription plan management `[Status: Planned / TO DO]`
- Account deletion option `[Status: Implemented]`

**Profile Management & Onboarding Flow** `[Status: Implemented]` (Navigation link is `[Status: Implemented]`)

This is a single route (`/dashboard/profile-management`) with three different presentations depending on the user's onboarding status. The three presentations are not three routes — they're one route with a server-side render branch based on `isOnboarded` and whether a CV parse result exists.

**The 3-Presentation State Machine:**

- **State 1 — CV Upload Form** (`isOnboarded=false`, no CV parsed):
  - This is the first page a new user sees after sign-up (default redirect target).
  - CV upload modal with mandatory CV naming field (`label`).
  - "Add New CV" prominent action button (Multiple CV upload allowed only for paid users).
  - List view of all uploaded CVs with edit/delete actions (post-onboarding).

- **State 2 — Onboarding Review** (`isOnboarded=false`, CV parse result in session):
  - After the user submits a CV and the Web Worker parsing + LLM extraction is complete.
  - Shows the LLM-extracted data (read-only summary with "edit" toggles for corrections).
  - Shows the LLM-proposed persona(s) — 1 or 2, with 5 `mustHaveTags` pre-filled.
  - User fills in mandatory user-collected fields (country, work preferences).
  - User confirms or adjusts the 5 skills per persona.
  - Single submit → creates `applicant` + `persona(s)` + `workingHistory` + `tagsExperience` + sets `isOnboarded=true`.
  - *Goal: under 3 minutes from upload to "onboarded."*

- **State 3 — Profile Management** (`isOnboarded=true`) `[Status: Implemented]`:
  - Same page, but now in "management mode" (user is already onboarded).
  - Work preferences section (country, `can_work_us_hours`, `assignment_types`, `modalities`, `preferred_compliance`) `[Status: Implemented]`.
  - Work history section (add/edit/delete job entries) `[Status: Implemented]`.
  - Skills section (view all — read-only) `[Status: Implemented]` — skills are derived from work history and kept in sync by `recomputeTagsExperience()`.
  - Persona section (edit existing, add up to 3, delete) `[Status: Implemented]`.
  - CV re-parse action (re-run LLM extraction on the latest CV) `[Status: Implemented]`.

**The 3 UI Sections (States 2 and 3):**

1. **Applicant Section**: Form reflecting user data collected from the CV with possibility to edit existing data and add new data. Also contains mandatory fields not present in the CV (country, `can_work_us_hours`, `assignment_types`, `modalities`, `preferred_compliance`). Editing employment history here is the only way to add/modify skills — skills are derived from work history, maintaining the single-source-of-truth principle.

2. **Skills Section**: Read-only list of all user skills extracted by the LLM, mapped against `CANONICAL_TAGS`. Users cannot add, delete, or deactivate skills directly; skills are derived from employment history. The content of this section is validated against the `CANONICAL_TAGS` array and is the single source of truth for the skillset selection form used in the Persona section.

3. **Persona Section**: One or multiple (max 3) personas (editable forms) based on one or multiple stacks derived from the user data and skills. Each stack contains core and optional skills, is based on exactly 5 skills (the "5 Major Skills" constraint), and is validated against `CANONICAL_TAGS`. The LLM proposes 1-2 initial personas based on the parsed CV data; the user then edits, adds, or deletes personas. Users must have at least one persona.

**Phase 1: Zero-Tax Client-Side Extraction & Server Action** `[Status: Implemented]`:
  - The browser loads `pdfjs-dist` in **main-thread "fake worker" mode** to extract raw text directly in the client, bypassing server memory limits. The original Web Worker approach was revised because browsers do not allow `pdfjs-dist` to spawn its own internal Worker from inside a custom Web Worker (nested Worker spawning fails silently, producing near-empty text). In fake-worker mode, `pdfjs-dist` detects `globalThis.pdfjsWorker.WorkerMessageHandler` and runs parsing in the same thread. For typical CV PDFs (1-5 pages), extraction takes <500ms on the main thread. See TDD §3.3 for the full architectural rationale.
  - *SSR Compatibility*: `pdfjs-dist` references browser-only APIs (`DOMMatrix`) at module evaluation time. The `extractPdfText()` function uses dynamic `import()` inside the function body so the library only loads in the browser, never during SSR.
  - *Server Action (Parse)*: The client component constructs a `FormData` with the raw text, label, and original filename, then calls the Server Action via `formAction(formData)` inside a `startTransition()` (required by `useActionState` in React 19). The Server Action calls `gpt-4o` via Vercel AI SDK `generateObject()` with `resumeExtractionSchema` (Schema 1). Application-level rate limiting: 3 parses/hour/user.
  - *System Prompt with Canonical Tags*: The `generateObject()` system prompt is built dynamically at runtime to include the full `CANONICAL_TAGS` and `PERSONA_DEFINING_TAGS` lists. Without the tag list, the LLM invents tag names that fail Zod schema validation. The prompt and schema read from the same source (`src/lib/jobs/tech-tags.ts`), so new tags automatically appear in both.
  - *Applicant Row Upsert*: The `parseCvAction` upserts an `applicant` row (with `onConflictDoNothing`) before inserting into `cv_upload`, because the FK constraint requires `applicant.user_id` to exist. First-time users have a Better Auth `user` record but no `applicant` row yet.
  - *Pre-LLM CV Validity*: Before calling the LLM, `validateCvRawText()` rejects raw text < 200 characters or text with no year-like patterns.
  - *AI Processing*: `gpt-4o` applies a Chain-of-Thought Overlap Merge Algorithm to merge overlapping employment date ranges. All extracted skills are normalized against the `CANONICAL_TAGS` dictionary (`src/lib/jobs/tech-tags.ts`). The LLM also proposes 1-2 personas (`proposed_stacks`) with 5 `must_have_tags` each.
  - *Persistence on Parse*: The `cvUpload` row is created immediately (status=`processing`), then updated with `extractedJson` and status=`valid` or `invalid` when the LLM completes. This survives page refreshes.
  - *Toast Notifications*: Sonner toasts provide prominent success/error feedback at the CV upload transition (green toast on successful parse, red toast with error message on failure).

**Phase 2: The Hybrid Mutation Pattern (Save & Vectorize)** `[Status: Implemented]`:
  - The complex dashboard form state is managed via **React Hook Form (RHF)** to handle drag-and-drop interactions and real-time client validation without performance-blocking re-renders.
  - Upon submission, RHF passes the validated data to a Next.js **Server Action** (`useActionState`). The `formAction(formData)` call is wrapped in `startTransition()` (required by React 19's `useActionState` for correct `isPending` tracking). The component constructs a `FormData` manually and calls `formAction` directly, rather than using `requestSubmit()` with hidden fields (which is unreliable in React 19 + Next.js 16).
  - *Post-Action Side Effects*: `router.refresh()` is called inside a `useEffect` that watches the `useActionState` result, not during render. This ensures the page re-render only fires once after the action completes.
  - *Inline Validation Errors*: The form displays validation errors at two levels: a summary error box listing all failed fields, and inline red error text next to each field (country, assignment types, modalities, preferred compliance, persona label/summary/must-have tags). RHF's `formState.errors` is passed to sub-components (`ApplicantSection`, `PersonaSection`) for field-level display.
  - *Toast Notifications*: Sonner toasts provide prominent success/error feedback at the onboarding completion transition (green toast "Onboarding complete!" on success, red toast with server error on failure).
  - *Strict Double-Validation*: The Server Action independently re-validates with `onboardingPayloadSchema.safeParse()` (Schema 2) before interacting with the database or generating `text-embedding-3-small` vectors.
  - *Transactional Re-aggregation*: `recomputeTagsExperience(applicantId)` runs inside a Drizzle PostgreSQL transaction — if it fails halfway, the entire operation rolls back to prevent persona corruption.
  - *Persona Embedding Auto-Regeneration*: When `mustHaveTags` change on any persona, the embedding is automatically regenerated via `text-embedding-3-small`.

**Onboarding Completion Rule**: The applicant's `isOnboarded` flag is set to `true` strictly when ALL of the following are satisfied:
- *User-collected*: `country`, `can_work_us_hours`, `assignment_types` (≥1), `modalities` (≥1), `preferred_compliance` (≥1)
- *LLM-extracted*: ≥1 employment entry with start/end dates, ≥3 canonical skills mapped to `CANONICAL_TAGS`
- *Derived*: ≥1 persona with exactly 5 `mustHaveTags`, `embedding_summary`, and generated `persona_embedding`
- *Experience level* is derived purely at query time from `tagsExperience.yearsOf_experience` (no stored enum field).

**Module A Post-MVP Items (Status Review)** `[Status: Complete — all items implemented or explicitly skipped]`:

The Module A onboarding MVP is complete. All pre-launch and post-launch follow-up items have been addressed: implemented for the critical path, or deliberately skipped because they are tied to paid-tier features. The rationale remains: Module B (ingestion) and Module C (matching) are the core product value, but the onboarding surface now supports the full lifecycle for an MVP launch.

| # | Item | Priority | Status | Rationale |
|---|------|----------|--------|-----------|
| P3 | Smart Dashboard Redirection | ✅ Done | Implemented | Two-layer redirect: signInAction + /dashboard page check isOnboarded |
| P1 | State 3 Full Editing | ✅ Done | Implemented | Full editing implemented in ProfileManagement: preferences, work history, personas, CV re-parse |
| P2 | Rate Limiting (3/hour) | ✅ Done | Implemented | 3 cvUpload rows/hour/user enforced in `parseCvAction` |
| P4 | Multiple CV Upload | ⏸️ Skipped | Post-launch / paid-tier | Feature expansion, not a launch gap; UI shows latest CV and supports re-parse |
| P5 | Orphaned Cleanup | ✅ Done | Implemented | `cleanupOrphanedCvUploads` Inngest cron job removes stuck processing + orphan cvUpload rows |

See TDD §3.9 for full technical detail on each item.

**Job Discovery & Management** `[Status: Implemented]` (Navigation link is `[Status: Implemented]`)

The `/dashboard/jobs` page is the primary calibration interface for the 3-Gate funnel. It displays matches from `match_queue` with all scoring data visible for threshold tuning. Two routes: a paginated list view and a detail view.

- **List view** (`/dashboard/jobs`):
  - Status filter tabs (Approved / Rejected / Pending / All) with per-status counts
  - Paginated match cards (10 per page) showing: job title, ATS source + slug, persona label, cosine distance, overlap score, LLM confidence (color-coded: green >0.7, yellow 0.4–0.7, red <0.4), LLM reasoning (1–3 sentences), blockers (as badges), status badge, unread indicator dot
  - "Mark all read" button (when unread approved matches exist)
  - "Mark as read" per-card action (Server Action + `router.refresh()`)
  - "View on ATS" link to the company's hosted career page
  - Empty state with link to profile management
  - Pagination controls (prev/next)
- **Detail view** (`/dashboard/jobs/[matchId]`):
  - Full job description parsed from `rawJson` via `extractJobContent` (ATS-source-aware extraction)
  - Gate 1+2 scores panel (cosine distance, overlap score, LLM confidence, timestamps)
  - Gate 3 LLM verdict panel (verdict, confidence, model, reasoning, blockers)
  - Persona context panel (label, embedding summary, must-have tags)
  - Extracted job tags with ✓ markers for tags overlapping persona must-haves
  - "View on ATS" link
  - Back to list navigation
- **Sidebar unread badge**: `getUnreadBadgeCount` fetched in the dashboard layout (Server Component), passed through to the sidebar nav. Updates on navigation and after `router.refresh()` from mark-as-read actions. Resilient — returns 0 on DB error instead of crashing the dashboard.
- **Calibration metrics exposed**: Cosine distance, overlap score, and LLM confidence are visible on both list and detail views. This is deliberate — the dashboard is the primary debugging interface for tuning `GATE2_MAX_COSINE_DISTANCE` and `GATE_ROUTER_LIMIT` against real data.
- **Dismiss button with reason capture (D20, July 2026)**: Replaces the old "Mismatch" button. `DismissButton` component (`src/components/dashboard/DismissButton.tsx`) with a dropdown of 9 dismiss reasons (`geo_fenced`, `wrong_stack`, `too_senior`, `too_junior`, `not_development`, `not_interested`, `stale`, `duplicate`, `other`). `dismissMatch(matchQueueId, reason)` Server Action sets `match_queue.status = 'mismatch'`, `dismiss_reason`, `dismissed_at`. The `dismiss_reason` enum + `dismissReason`/`dismissedAt` columns added via migration 0056. `DISMISS_REASONS` const + `DismissReason` type live in `src/lib/jobs/match-filters.ts` (client-safe — NOT in the `"use server"` module, which can only export async functions). 11 existing "mismatch" rows backfilled with `dismiss_reason='other'`. The dismiss-reason breakdown feeds into the North Star daily report for classifier improvement.
- **North Star daily report (D20, July 2026)**: `northStarDailyReport` Inngest function (cron `0 7 * * *`) tracks the project's proof gate metric. Reports: approved matches today (total, unique users, unique personas), would-apply matches, dismissals with full reason breakdown, 7-day rolling would-apply average, tripwire status (≥5 would-apply/day × 7 consecutive days), corpus health (matchable jobs, unembedded active, fenced with embedding). Emits `north-star/daily` event. The tripwire is the project's definition of "product-market fit found" — when the founder would apply to ≥5 jobs/day for 7 consecutive days, the matcher has earned the right to scale beyond solo use.
- **Post-MVP features** `[Status: Planned / TO DO]`: Search across job attributes, salary range filtering, archive/delete with 30-day retention, bulk actions, application tracking.

### Ingestion, Seeding & Routing Pipeline `[Status: Implemented]` — Module B (ingestion) is Implemented & Live-Tested; Module C (3-Gate routing) is Implemented (synthetic-data calibrated, real-data calibration launch-blocking)

#### 1. The Seeding & Ingestion Engine (Module B) `[Status: Implemented & Live-Tested ✅]`

The system is fully autonomous — no human-in-the-loop for routine operations. Unresolvable discoveries are discarded, not queued for manual review. Full technical specification in TDD §4.

**Database Tables (Module B):**
- **`company` table** `[Status: Implemented]` — The ATS slug registry. Stores discovered `(ats_slug, ats_source)` tuples with tier (active/dormant/dead), health status, polling state, and discovery provenance. Unique constraint on `(ats_source, ats_slug)`. Drizzle path: `src/db/schemas/jobs/company.ts`.
- **`ingestionLog` table** `[Status: Implemented]` — Observability. Every seeder and poller run is logged with metrics (items processed/inserted/rejected/skipped), error details, and duration. Drizzle path: `src/db/schemas/jobs/ingestionLog.ts`.
- **`job` table updates** `[Status: Implemented]` — Adds `external_job_id` (for dedup via upsert), `last_seen_at` (for stale detection), and `status` (active/stale/gone). New unique index on `(ats_source, ats_slug, external_job_id)`. Drizzle path: `src/db/schemas/jobs/job.ts`.

**Seeders (Discovery):**
- **Monthly BigQuery Volume Seed** `[Status: Implemented]`:
  - Run queries in Google BigQuery against the public `httparchive` dataset using the BigQuery MCP integration. Sandbox Mode (1 TB/month free, no billing required).
  - **⚠️ The `httparchive.technologies` table no longer exists** (reorganized April 2025). Data now lives in `httparchive.crawl.pages` as a nested `technologies.technology` array. All queries must pin a specific monthly `date` partition (30 TB/month table).
  - Detect domains running modern web tech (4 tiers: core frameworks, backend/runtime, build tools/CSS, legacy detectable stacks like PHP/WordPress/Laravel/Rails) whose pages integrate Greenhouse, Lever, or Ashby widgets.
  - **HTTPArchive homepage-only workaround:** Two-phase approach — (1) BigQuery finds candidate domains by tech stack + REGEXP_EXTRACT for direct slug extraction, (2) slug probe resolver handles domains where the slug couldn't be extracted. No HTML scraping.
  - **AI-Assisted Analysis**: Use `ask_data_insights` and `search_catalog` MCP tools for natural language exploration of HTTP Archive data and rapid prototyping of discovery queries.
  - Implementation: domain logic (`src/lib/jobs/seeders/bigquery-seeder.ts`) with injectable BQ client, manual script (`scripts/seed-bigquery.ts`), and Inngest scheduled function (`bigQuerySeeder`, monthly cron `0 0 1 * *`).
- **Weekly Hacker News Algolia Delta Seed** `[Status: Implemented]`:
  - Automatically parse the monthly "Who is Hiring" threads on Hacker News using the Algolia search API. This is the primary "hidden jobs" discovery engine — surfaces 200–500 companies per month, many first-time posters or small startups not in HTTPArchive.
  - Extracts direct ATS URLs (`boards.greenhouse.io/{slug}`, `jobs.lever.co/{slug}`, `jobs.ashbyhq.com/{slug}`) and non-ATS URLs (`mystartup.com/careers`).
  - **Non-ATS URL resolution (autonomous):** Two-stage — (1) DNS CNAME check, (2) slug probe against all three ATS APIs. If both fail, discard the URL — no manual review.
  - Implementation: Inngest scheduled function (`hnAlgoliaSeeder`, weekly cron `0 0 * * 1`) + event-driven custom URL resolver (`customUrlResolver`).
- **crt.sh Stealth Seeder** `[Status: Implemented — Sprint 4, June 30 2026]`:
  - New `src/lib/jobs/seeders/batch-sources/crt-sh.ts` queries Certificate Transparency logs via `crt.sh/?q=%25.boards.greenhouse.io&output=json` for historical ATS domain discoveries. Replaces disabled Rapid7 FDNS. Monthly refresh cron.

**The "Phalanx" Poller** `[Status: Implemented]`:
- An Inngest-driven background worker dynamically processes collected ATS slugs. Three optimizations for production scalability:
  - **Optimization 1 (Concurrency):** Inngest capped at 50 concurrent steps. `bottleneck` enforces 2 req/s per ATS platform (`maxConcurrent: 1, minTime: 500`).
  - **Optimization 2 (Compute separation):** Poller only fetches JSON + inserts to Postgres. AI embeddings deferred to Module C (`job/ingested` event).
  - **Optimization 3 (Decay polling):** Tier A (active, job posted in last 14 days) → poll every 12h. Tier B (dormant, no jobs in >14 days) → poll weekly. Tier C (dead, 404 or 3+ consecutive failures) → stop polling. Tiers recalculated daily by a scheduled Inngest function.
- **Fan-out architecture:** Two scheduled functions (`tierActiveFanOut` every 12h, `tierDormantFanOut` weekly) emit `poller/poll-company` events. Each event triggers a separate `pollCompanyFn` instance (concurrency cap 50). No per-company scheduled functions.
- Pulls job specs from native public JSON endpoints for Greenhouse, Lever, and Ashby (centralized in `src/lib/jobs/ats-endpoints.ts`). ATS adapters (`src/lib/jobs/poller/ats-adapters.ts`) fetch + Zod validate + normalize to a unified `NormalizedJob` shape.
- **Gate 0 (pre-filter):** Synchronous regex title filter rejects non-engineering jobs before database insertion. Optimized for recall — the 3-Gate funnel handles precision.
- **Defensive Zod schemas:** Every ATS response validated with `safeParse()`. Payload changes degrade gracefully (company flagged as `degraded`) rather than crashing the worker.
- **Deduplication:** Upsert on `(ats_source, ats_slug, external_job_id)` unique constraint. Re-polls refresh `lastSeenAt` and `rawJson`. New jobs detected for the B→C handoff event.
- **Stale job cleanup:** Daily Inngest function (`staleCleanup`, cron `0 3 * * *`) marks jobs not seen in 7 days as `stale`, not seen in 30 days as `gone`. Module C only matches `status = 'active'` jobs.
- **B→C handoff:** Poller emits `job/ingested` Inngest event only for genuinely new jobs (not upserts). Module C owns normalization (tag extraction + embedding). **Sprint 7 (July 1 2026):** `batchPollTier` now also emits `job/ingested` events for unnormalized jobs found via DB query after polling — a robust fallback that works even when the poll step times out and retries (jobs already in DB → `newJobIds: []`, but DB query still finds them as unnormalized). The `normalizationRetrySweep` daily cron (06:00 UTC, limit 500/run) catches any remaining stuck jobs (`active` + `normalizedAt IS NULL` OR `normalization_failed`). **Sprint 8 (July 1 2026):** `matchBulkReprocess` (event-triggered, manual via admin dashboard) retroactively matches existing jobs against personas. `matchRetrySweep` (daily cron 07:00 UTC) catches jobs missed by the normal pipeline — queries active+embedded jobs older than 1h with no match_queue entry. `personaUpdatedHandler` now triggers bulk reprocess for new jobs after persona changes.
- **Company health tracking:** `updateCompanyState()` tracks `consecutiveFailures` — after 3 consecutive failures, company is auto-marked `dead` and `pollingEnabled = false`. HTTP status codes map to health states (429→rate_limited, 403→blocked, 404→dead, 500+→error).
- **Automated endpoint health monitoring & LLM recovery** `[Status: Planned / TO DO]`: Periodic endpoint probing detects API changes. When an endpoint degrades, an LLM-powered recovery function researches the ATS provider's current docs, proposes and tests a new endpoint, and updates the registry programmatically.
- *Proxy Routing Fallback* `[Status: Planned / TO DO — Post-MVP]`: Deferred. Rate limiter is sufficient for MVP. Trigger to add: first persistent 403 from an ATS.

#### Neon Database Impact Analysis (Module B) `[Status: Historical — superseded by VPS Postgres migration (D20, July 20 2026)]`

> **⚠️ HISTORICAL NOTE (D20, July 20 2026):** This section documents the Neon free-tier storage and compute analysis that drove the D16-D17 cron freeze. The app database was migrated to VPS Postgres 17 on July 20 2026, eliminating the Neon CU-hr burn constraint (94.35% of 100 CU-hrs used at the crisis point) and the 512MB storage limit. The analysis below is preserved for historical context and disaster-recovery reference. The safeguards implemented (Gate 0 title filter, deduplication, stale/gone cleanup, decay polling, G7 rawJson pruning, G5+G6 batch polling) remain in effect on VPS Postgres — they are good practice regardless of the database host. The Neon-specific considerations (autosuspend, connection pooling, branching) no longer apply.

**Concern:** The seeders and poller populate the `company` and `job` tables. Unbounded growth could exhaust Neon storage, connection pool, or compute limits. This section documents the expected scale and the safeguards in place.

**Expected row counts (steady-state estimates):**

| Table | Source | Estimated Rows | Rationale |
|-------|--------|---------------|-----------|
| `company` | BigQuery monthly | ~2,000–5,000/month | HTTPArchive finds ~2k–5k domains per monthly crawl running target tech stacks with ATS script URLs. Many will dedup against existing rows (`onConflictDoNothing` on `atsSource+atsSlug`). |
| `company` | HN Algolia weekly | ~200–500/month | HN "Who is Hiring" surfaces 200–500 companies per month. Most are first-time posters not in HTTPArchive. Dedup via `onConflictDoNothing`. |
| `company` | Corpus Expansion (batch flush) | 5,290 (June 29 2026) → 10,114 (July 7 2026) | One-time flush from 10 batch sources + daily source accumulation + corpus-persona alignment. Top source: Wayback CDX (4,163). YC Directory (374), Newsletter Archives (257), VC Portfolios (36), Cross-Pollination (11). Exceeded 5,000 target by 106%. Grew to 10,114 via daily sources + BigQuery re-runs by July 7 2026. |
| `company` (total) | All sources | 10,114 (actual, July 7 2026) | Registry has stabilized at ~10K. Tier distribution: 950 active_hot, 9,126 active, 36 dormant, 2 dead. 1,431 companies have employee_count populated. Dead companies are auto-disabled (`pollingEnabled = false`) but rows are NOT deleted (preserves discovery history). |
| `job` | Phalanx Poller | ~50,000–150,000 active (estimated steady-state) | Each company has 5–50 open engineering roles on average. Gate 0 filters out ~60–70% of jobs (non-engineering titles). After stale/gone cleanup, the active set stays bounded. **Current actual (July 7 2026):** 5,436 total jobs, 2,405 active, 4,639 normalized. |
| `job` (total including stale/gone) | Poller over time | ~200,000–500,000 (estimated) | Stale jobs (7 days) and gone jobs (30 days) are NOT deleted — they're kept for match history and potential resurrection. This is the long-term growth table. |

**Storage estimate:** At ~500 bytes per job row (title + normalizedText + metadata, after G7 rawJson pruning), 500k rows ≈ 250 MB. Neon's free tier includes 3 GB storage; the paid tier (Launch) starts at 10 GB. The `job` table will not exceed Neon limits for years. **G7 backfill (June 29 2026):** 4,491 jobs processed, rawJson NULLed, ~31 MB reclaimed (rawJson was ~15KB/job, normalizedText is ~3KB/job — 80% reduction per row).

**Safeguards implemented:**

1. **Gate 0 title filter (pre-insertion):** Synchronous regex rejects ~60–70% of jobs (non-engineering titles like "Account Executive", "HR Manager") *before* they touch the database. This is the primary volume control — without it, the `job` table would be 3x larger.
2. **Deduplication via upsert:** The `onConflictDoUpdate` on `(atsSource, atsSlug, externalJobId)` ensures re-polls update existing rows rather than inserting duplicates. A company polled 100 times produces the same number of rows as one poll.
3. **`onConflictDoNothing` on seeders:** The company table uses `onConflictDoNothing` on `(atsSource, atsSlug)` — re-discovering the same company doesn't overwrite its polling state or health data.
4. **Stale/gone cleanup (daily):** Jobs not seen in 7 days → `stale`, 30 days → `gone`. Module C only matches `status = 'active'` jobs. This keeps the *active* set bounded (~50k–150k) even as the total table grows.
5. **Decay polling (tier-based):** Dead companies (3+ consecutive failures) are auto-disabled (`pollingEnabled = false`). Dormant companies (no jobs in 14 days) are polled weekly, not every 12h. This reduces poll volume by ~80% vs. polling all companies daily.
6. **Rate limiting (bottleneck):** 2 req/s per ATS platform prevents overwhelming the Neon connection pool. Inngest's concurrency cap (5, free plan limit) limits simultaneous poll function instances.
7. **Compute separation:** The poller only fetches JSON + inserts to Postgres. AI embeddings (expensive, slow) are deferred to Module C's `job/ingested` handler. This keeps poll transactions fast and connection-pool-friendly.
8. **No deletion policy:** Stale/gone jobs are status-flagged, not deleted. This preserves match history (FK integrity for `matchQueue`) and enables resurrection if a company re-posts the same job. Deletion would be premature optimization at this scale.
9. **G7 rawJson pruning (June 29 2026):** After normalization, the job's `rawJson` (~15KB) is NULLed and the cleaned text is stored in `normalizedText` (~3KB) — an 80% storage reduction per job. The `normalizedText` column was added via migration 0016. A one-time backfill script (`scripts/backfill-normalized-text.ts`) processed 4,491 existing jobs, reclaiming ~31 MB. New jobs are pruned automatically by `jobIngestedHandler` during normalization.
10. **G5+G6 batch polling (June 29 2026):** The per-company fan-out architecture (1 Inngest function per company poll) was replaced with batch polling — `batchPollTier` polls 100 companies per function run, reducing Inngest execution count by 50-100x. This makes 5,000+ companies viable on the Inngest Hobby plan (50K executions/month). Three cron triggers map to three tiers: `active_hot` (every 3h), `active` (every 12h), `dormant` (weekly Monday 3am).
11. **Storage monitoring — dual-threshold strategy (July 4 2026):** Neon's `synthetic_storage_size` (the enforced limit) is ~12% larger than `pg_database_size()` because it includes WAL, history retention, and overhead. The app uses a dual approach: (a) **Hot-path ingestion guard** uses `pg_database_size()` with `STORAGE_LIMIT_MB = 460` (safety margin below the 512 MB hard limit) — fast, no external API call. (b) **Hourly storage monitor** uses the Neon API (`synthetic_storage_size`) with the true 512 MB limit — accurate, runs hourly with a 60s cache. Env vars: `NEON_API_KEY`, `NEON_PROJECT_ID`. Module: `src/lib/jobs/neon-api.ts`.
12. **Emergency storage purge with WAL inflation protection (July 4 2026):** `emergencyStoragePurge` Inngest function (cron every 15 min) runs a 5-tier deletion strategy (normalization_failed → rejected → gone → stale → active FIFO) when storage exceeds 88% of `STORAGE_LIMIT_MB`. The purge uses `STORAGE_LIMIT_MB` (not 512) for recovery checks and includes **WAL inflation detection** — if storage increases for 2 consecutive batch checks (DELETE generating more WAL than it reclaims), the purge aborts immediately to prevent a death spiral. The `active_fifo` tier uses a smaller batch size (500 vs 1000) to limit per-batch WAL generation. **Per-batch VACUUM (July 2026 fix):** VACUUM ANALYZE runs after each batch (not just between tiers) so `pg_database_size()` reflects reclaimed space at the next recovery check — without this, dead tuples are still counted as used space and the recovery threshold check never fires within a tier, causing false "not recovered" alerts even when storage dropped well below the threshold. **Active FIFO safeguards (July 2026):** The last-resort `active_fifo` tier has three protections to prevent corpus wipe: (a) **48h age protection** — jobs younger than 48 hours are excluded (haven't had time to be normalized and matched); (b) **normalized-only** — jobs with `raw_json IS NOT NULL` (still in the normalizer's queue) are excluded, decoupling the purge from the backlog; (c) **corpus percentage guard** — at most 20% of the active corpus can be deleted per purge run (`PURGE_ACTIVE_FIFO_MAX_CORPUS_FRACTION = 0.2`); if exceeded, the purge aborts with a "corpus_percentage_guard" alert instead of silently destroying data. Module: `src/lib/jobs/poller/cleanup-queries.ts`.
13. **Simplified storage alerting (July 4 2026):** Storage alerts now create database records only (email notifications removed). The alerting logic focuses solely on storage percentage — unnormalized backlog checks were removed from `checkStorageAlerts()` and are handled by the separate `pipelineHealthMonitor`. Thresholds: warning at 80%, critical at 88% of `STORAGE_LIMIT_MB`.

**Neon-specific considerations:**
- **Autosuspend:** Neon's serverless compute suspends after 5 minutes of inactivity. The first poll after suspension incurs a ~300ms cold-start penalty. This is acceptable for a background poller (not user-facing).
- **Connection pooling:** Neon's pooled connection string (`-pooler` suffix) should be used for the poller's DB connections. The Inngest concurrency cap (50) + bottleneck rate limiting ensures we never exceed Neon's connection limit.
- **Branching:** Neon branches can be used for testing seeders/poller against a copy of production data without affecting the live database.

**When to revisit:** If the `job` table exceeds 1M rows, consider (1) archiving `gone` jobs to a cold storage table, (2) partitioning the `job` table by `detectedAt` month, (3) adding a TTL policy for `gone` jobs older than 1 year. None of these are needed for MVP. **Note (D20):** The VPS Postgres migration removed the Neon storage ceiling — the 80GB Hetzner disk is now the constraint, which is far more generous than Neon's 512MB free-tier limit.

#### Module B Testing Strategy `[Status: ✅ PASSED — All 3 Layers Complete + Corpus Expansion (June 2026)]`

Module B is feature-complete with 2,640 unit/integration tests passing across 119 test files (includes corpus expansion, corpus-persona alignment, storage monitoring, Gate 0.5, work authorization, WAL inflation protection, per-batch VACUUM regression test, corpus percentage guard test, company scorer, remote-scope extractor, circuit breaker, frontend job scanner, direct job board ingestion, backlog sweeper, mismatch analysis 6-fix cascade, generalized recurrence guard, and all original seeder/poller tests). All 3 live testing layers have been completed against real ATS APIs and a real Neon dev branch (`module-b-testing`). The corpus expansion batch flush was executed against the production Neon database (June 29 2026) with 5,290 companies inserted. The corpus-persona alignment backfills were applied July 7 2026, growing the registry to 10,114 companies. Direct job board ingestion (6 boards) and backlog sweeper added July 2026.

**Testing approach (3 layers, all PASSED):**

**Layer 1 — Live ATS API smoke test (no DB): ✅ PASSED**
- Ran the ATS adapters against known-active slugs per platform (Greenhouse, Lever, Ashby).
- Command: `npx tsx scripts/smoke-ats-apis.ts`
- Verifies: Zod schemas, normalization, rate limiter integration
- Result: All 3 ATS platforms returned valid responses that passed Zod `safeParse()`.
- **Bug found & fixed:** Greenhouse `metadata[].value` can be boolean instead of string — schema updated to `z.any()` with regression test.

**Layer 2 — HN Algolia seeder live run (with DB): ✅ PASSED**
- Ran the HN seeder against the real HN Algolia API with the Neon dev branch.
- Command: `npx tsx scripts/seed-hn-live.ts`
- Verifies: HN API parsing, URL extraction, ATS classification, company table insertion, dedup
- Result: 501 comments processed, 206 ATS URLs found, 60 unique companies inserted, 782 custom URLs queued for resolver.
- **3 bugs found & fixed:**
  1. **HTML entity decoding** — HN Algolia returns `&#x2F;` for `/`, making URLs unparseable. Added `decodeHtmlEntities()` to `extractUrls()` in `url-parser.ts`.
  2. **Two-phase HN fetch** — The broad full-text search matched "Who wants to be hired" threads (job seekers, no ATS URLs). Changed to find the "Who is hiring?" story by `whoishiring` author, then fetch comments by story ID.
  3. **`job-boards.greenhouse.io` hostname** — Alternate Greenhouse board URL not in `ATS_HOST_PATTERNS`. Added with same slug extractor as `boards.greenhouse.io`.

**Layer 3 — Inngest Dev Server integration test: ✅ PASSED (all 5 sub-layers)**

**Layer 3a — Function sync: ✅ PASSED**
- All 10 Inngest functions synced with the Dev Server: `seeder-hn-algolia`, `seeder-resolve-custom-url`, `seeder-bigquery`, `poller-poll-company`, `poller-tier-active-fanout`, `poller-tier-dormant-fanout`, `phalanx-poller`, `poller-tier-recalc`, `poller-stale-cleanup`, `job-ingested-handler`.

**Layer 3b — HN seeder via Inngest: ✅ PASSED**
- Triggered `seeder-hn-algolia` via `npx inngest-cli@latest api invoke-function`.
- All 3 steps completed: `fetch-and-insert` (8s), `write-log` (0.7s), `emit-custom-url-resolution` (8ms).
- ingestionLog: `[seed] success — processed: 501, inserted: 0, skipped: 60` (duplicates from Layer 2).
- `seeder/resolve-custom-url` event emitted successfully.

**Layer 3c — Per-company poll: ✅ PASSED**
- Triggered `phalanx-poller` for vestwell (Greenhouse) and 3 other companies (livekit, permitflow, weave).
- All 3 steps completed: `get-company`, `poll-company`, `emit-job-ingested`.
- Result: 30 jobs fetched, 1 passed Gate 0 (Staff Software Engineer), 29 rejected (non-engineering).
- Job inserted with `extractedTags: []`, `jobEmbedding: null` (Module B → C handoff).
- Company state updated: `lastPolledAt`, `activeJobCount`, `health: healthy`.
- `job/ingested` events emitted, triggering `job-ingested-handler` (Module C entry point).
- Total: 22 jobs inserted across 4 companies (livekit: 13, permitflow: 7, weave: 1, vestwell: 1).

**Layer 3d — Tier recalc + stale cleanup: ✅ PASSED**
- `poller-tier-recalc`: 60 companies updated — 4 → active (recently polled), 56 → dormant.
- `poller-stale-cleanup`: COMPLETED (0 jobs marked stale/gone — all freshly inserted).
- **Bug found & fixed:** Tier recalc raw SQL `UPDATE` failed with `column "tier" is of type company_tier but expression is of type text`. Fixed by casting each CASE branch to `::company_tier`.

**Layer 3e — Fan-out pattern: ✅ PASSED**
- Triggered `poller-tier-active-fanout` — queried active-tier companies due for polling.
- Emitted `poller/poll-company` event for vestwell (reset `lastPolledAt` to null to make it "due").
- `poller-poll-company` function triggered by the event, polled vestwell, completed successfully.
- ingestionLog: `[poll] success — processed: 30, inserted: 0, updated: 1, rejected: 29`.
- Full fan-out → poll-company → job-ingested chain verified.

**Test data cleanup:** Used Neon dev branch `module-b-testing` (copy of production schema, empty data). Branch can be discarded — no cleanup needed on the production database.

**Bugs found and fixed during live testing (4 total):**
1. Greenhouse `metadata[].value` schema drift (boolean instead of string) — `ats-schemas.ts`
2. HN Algolia HTML entity encoding breaking URL extraction — `url-parser.ts`
3. HN Algolia search strategy matching wrong threads — `hn-algolia.ts`
4. PostgreSQL enum cast in tier recalc UPDATE — `tier-queries.ts`

All fixes include regression tests. Total test count: 48 seeder URL-parser tests + 10 HN Algolia tests + 11 ATS schema tests + 20 poller tests = 89 Module B tests (all passing).

#### 2. The 3-Gate Event-Driven Matching Engine (Module C) `[Status: Implemented — Real-Data Calibrated, Gate 2 Re-Architected to Rank Signal (D18 July 2026)]`

When a new job listing is successfully ingested, an asynchronous workflow is triggered by Inngest. Full technical specification in TDD §5 and `docs/reports/MODULE_C_DECISIONS.md`.

> **D18 Re-Architecture (July 2026):** Gate 2 was re-architected from a hard threshold gate to a rank signal. The bypass test revealed that the embedding representation has a granularity mismatch — persona embeddings are 3-sentence summaries while job embeddings are title + full description. Perfect matches sit at cosine distance 0.45-0.55, not 0.20-0.35. No threshold cures this. With `GATE2_RANK_ONLY=true` (default), jobs that pass hard filters + stack match are ALL inserted into match_queue, ordered by semantic distance. This produced the project's first completed end-to-end promise — the founder applied to 2 jobs (ruby-labs, runway-ml) in the first run.

- **Trigger**: Inngest emits a `job/ingested` event (only for genuinely new jobs, not upserts).
- **Step 1 (Job Normalization & Vectorization)** `[Status: Implemented]`:
  - `jobIngestedHandler` in `src/inngest/functions.ts` receives the event.
  - Idempotency guard: if `job.normalizedAt IS NOT NULL`, skip (event re-delivery is safe).
  - `src/lib/jobs/job-normalizer.ts` extracts canonical tech tags: regex dictionary scan first (`scanTagsRegex`), LLM fallback (`extractTagsLLM` via `gpt-4o-mini`) if regex yields <3 tags. Ashby description prefers `descriptionPlain` over `descriptionHtml`.
  - `decideNormalizationAction` handles rejection (garbage job → `status = 'rejected'`) vs. system failure (`status = 'normalization_failed'`).
  - `src/lib/jobs/job-embedder.ts` generates a 1536-d embedding via `text-embedding-3-small`.
  - Sets `normalizedAt` on terminal success or rejection (never on `normalization_failed`).
- **Step 2 (Fast Routing via Gate 1 & Gate 2)** `[Status: Implemented]`:
  - `src/lib/jobs/gate-1-2.ts` runs a single-pass SQL query (`runGateSQLRouter`) comparing the job against all personas:
    - **Gate 1 (Exact Filtering)**: GIN index array overlap via `&&` operator with minimum overlap threshold:
      ```sql
      (p.must_have_tags && jobTags) AND NOT (p.blocklist_tags && jobTags)
      AND ov.overlap_score >= 2  -- GATE1_MIN_OVERLAP (added July 2026)
      ```
      `GATE1_MIN_OVERLAP = 2` (env-configurable via `GATE1_MIN_OVERLAP`) filters out 1-tag-overlap matches that Gate 3 was approving. Added July 2026 based on mismatch analysis: 16 of 50 user-marked mismatches had `overlap_score = 1`.
    - **Gate 2 (Semantic Similarity — RANK, not GATE since D18 July 2026)**: HNSW cosine distance, but re-architected from a hard threshold gate to a rank signal. The bypass test (D18) revealed that the embedding representation has a granularity mismatch — persona embeddings are 3-sentence summaries (50-500 chars) while job embeddings are title + full description (thousands of chars). Perfect matches sit at cosine distance 0.45-0.55, not 0.20-0.35. The spread between perfect and terrible is only 0.20 — no threshold cures this. The representation is the disease.
      - **Rank-only mode (default, `GATE2_RANK_ONLY=true`):** Uses `GATE2_HARD_CEILING=0.75` as a wide safety net. Jobs that pass hard filters (scope, fence, natsec, qa) + stack match are ALL inserted into match_queue, ordered by semantic distance. The cosine cliff is eliminated — a qualified job is never dropped for being 0.0036 over a line; it's shown, ranked lower. Produced 19.7x increase in candidates (3 → 59 match_queue entries). Founder applied to 2 jobs in the first end-to-end run.
      - **Legacy gate mode (`GATE2_RANK_ONLY=false`):** Uses the strict `GATE2_MAX_COSINE_DISTANCE` threshold (default 0.50, env-configurable). Historical values: 0.50 (Sprint 3+), 0.48 (June 28 2026), 0.55 (June 25 2026), 0.35 (blocked all real matches). D16 adopted 0.55 as the founder ruling, but the `.env` override `GATE2_MAX_COSINE_DISTANCE=0.50` was discovered in D18 to be overriding the code default — production was running at 0.50, not 0.55, blocking 22 perfect matches at cosine 0.50-0.55.
      - **Empty embedding handling (July 2026 fix):** Jobs with NULL/empty embeddings (probation companies whose backfill hasn't run, or intentionally-fenced jobs whose embedding was skipped to save OpenAI calls) now return `[]` — they are NOT matched. Previously, a `runGate1Only` fallback let unembedded jobs bypass Gate 2 entirely, producing 44 of 50 user-marked mismatches. The `runGate1Only` function is retained but no longer called from `runGateSQLRouter`.
    - **Composite ordering**: `overlap_score * 0.6 + (1 - cosine_distance) * 0.4 DESC` — blends both signals so a strong semantic match isn't outranked by a barely-passing tag-overlap match.
    - **Overlap count**: Uses `unnest` + `= ANY` in a LATERAL subquery (NOT the `&` operator, which only exists for `intarray`/`integer[]`, not `text[]`).
    - Top 8 candidates (`GATE_ROUTER_LIMIT`) inserted into `match_queue` with `ON CONFLICT (job_id, persona_id) DO NOTHING`.
    - **Idempotency trap fix (D18 July 2026):** `jobIngestedHandler` now has a "route-only" recovery path. When a job is already normalized + embedded but has no match_queue entries (crash between Step 4 and Step 5), the handler runs the gate router directly instead of skipping. Catches jobs that fell through the idempotency trap.
    - **COALESCE default-false bug fix (D19 July 2026):** `is_fenced`/`is_natsec`/`is_qa` columns changed from `DEFAULT FALSE` to `DEFAULT NULL` (migration 0055). The FALSE default killed the COALESCE regex fallback in `gate-1-2.ts` — with FALSE, no row was ever NULL, so the inline regex backstop was dead code. The NULL default lets un-backfilled jobs fall through to the regex fallback correctly. D19 backfill: 2,926 jobs fenced.
  - `jobIngestedHandler` fans out one `match/gate-3-evaluate` Inngest event per candidate row.
- **Step 3 (Gate 3 - High-Fidelity AI Arbitration)** `[Status: Implemented]`:
  - `gate3Evaluator` Inngest function in `src/inngest/functions.ts` receives `match/gate-3-evaluate` events.
  - `src/lib/jobs/gate-3.ts` builds a structured prompt (`buildGate3Prompt`) with job description, persona context, and applicant constraints (country, work hours, compliance, modalities, assignment types, **seniority levels**, **work authorizations**).
  - `evaluateGate3` calls `gpt-4o-mini` via Vercel AI SDK `generateObject` with a strict Zod schema (`gate3VerdictSchema`) — verdict (`approved`/`rejected`), confidence (0.0–1.0), reasoning, blockers, and **workAuthRiskFlag**.
  - **A/B test prompt variants** (added June 28 2026): Three variants (`balanced`, `strict`, `thorough`) randomly assigned per candidate and stored in `match_queue.prompt_variant` for later analysis of approval rates.
  - **Seniority-aware matching** (added June 28 2026): `Gate3Context.applicant.seniorityLevels` passed to the LLM — rejects jobs whose inferred seniority doesn't match the user's selected levels.
  - **Country-specific remote restrictions** (added June 28 2026, restructured July 2026): All prompt variants instruct the LLM to scan for geographic limitations (e.g., "remote (US only)") and reject country mismatches as hard blockers. **Compliance directive restructured (July 2026 mismatch fix):** The directive now uses a 2-step structure — Step 1 checks if the job is country-fenced to a list that does NOT include the US (HARD BLOCKER — w8ben only covers US contractor arrangements); Step 2 applies the "default to approving" rule only for US-included restrictions. The previous "OVERRIDE — DEFAULT TO APPROVING" label was too prominent and caused the LLM to apply the w8ben rule to ALL countries (CA, PL, PT, MX, IN, CL, CR), producing 15 of 50 mismatches.
  - **Management/PM role detection (July 2026 mismatch fix):** All 3 prompt variants now include criterion 8 — if the job title contains management keywords ("Manager", "Engineering Manager", "Director", "Head of", "VP", "Chief", "Product Manager", "Program Manager", "Tech Lead", "Team Lead") AND the persona's seniority levels are IC-only (mid, junior, senior — without lead, staff, principal, manager), this is a HARD BLOCKER. Addresses 6+ of 50 mismatches where management/PM roles were approved for IC developer personas.
  - **Work authorization filtering + risk flagging** (added July 4 2026): `Gate3Context.applicant.workAuthorizations` passed to the LLM — supports 10 permit types (eu_citizen, rwr_card_plus, blue_card_eu, uk_settled, uk_pre_settled, us_green_card, us_citizen, canadian_pr, swiss_permit_c, other_permit). The LLM checks job work-auth requirements against the applicant's permits (hard blocker if required permit is missing). A `workAuthRiskFlag` boolean is set when the JD is silent on work authorization but the role is hybrid or single-country-remote (not global) — warns the user to verify before applying. A dynamic work authorization directive is injected into the prompt when the applicant has permits set. The `workAuthRiskFlag` field in `gate3VerdictSchema` must NOT use `.default()` — OpenAI's strict JSON schema mode requires all properties in the `required` array.
  - **Geo-deduction hard-blocker rules (D19 July 2026):** The founder's manual geo-deduction logic encoded into the Gate 3 prompt — if a job says "Remote - US" or "Remote - India", it's country-fenced regardless of what the `remote_scope` field says. Explicit hard-blocker rules prevent the LLM from approving country-fenced jobs for applicants without work authorization in that country.
  - Verdict written to `match_queue` (`llm_verdict`, `llm_reasoning`, `llm_confidence`, `llm_blockers`, `llm_model`, `prompt_variant`, `evaluated_at`). If approved, `match/approved` event emitted (MVP: nothing listens — dashboard polls directly; Module D will consume this post-MVP).
  - Error recovery: unparseable output or exhausted retries → `status = 'pending'`, `llm_verdict = 'error'` (recoverable by `pendingQueueSweep` Inngest function).
  - **Gate 3 feedback loop** (added June 28 2026): `personaUpdatedHandler` Inngest function re-evaluates rejected `match_queue` rows when a user updates their persona tags or embedding summary. `pendingQueueSweep` (cron every 15 min) picks up stuck pending rows.
- **Step 4 (Dashboard Notification Layer)** `[Status: Implemented]`:
  - `src/lib/jobs/dashboard-queries.ts`: `getMatches` (status-filtered, paginated, applicant-scoped), `getMatchesCount` (for pagination), `getApprovedMatches` (backward-compat wrapper), `getUnreadBadgeCount` (resilient — returns 0 on DB error), `getMatchDetail` (includes `rawJson`, `llmConfidence`, `llmBlockers`).
  - `src/actions/matches.ts`: `markMatchRead`, `markAllMatchesRead` Server Actions.
  - `/dashboard/jobs` page: Server Component with status filter tabs, paginated match cards, "Mark all read" button, empty state. All calibration metrics (cosine distance, overlap score, LLM confidence) visible on cards.
  - `/dashboard/jobs/[matchId]` page: Full detail view with job description (parsed from `rawJson`), LLM reasoning + blockers, Gate 1+2 scores, persona context, "View on ATS" link.
  - Sidebar unread badge: Wired via dashboard layout → `DashboardSidebar` → `DashboardSidebarNav`.
- **Calibration (Feature C6)** `[Status: Implemented — Real-Data Calibrated (Self-Use)]`:
  - `scripts/calibrate-routing-engine.ts` measures Gate 1+2 output distributions and Gate 3 verdict quality against seed data.
  - `docs/reports/calibration-report.md` documents findings: `GATE2_MAX_COSINE_DISTANCE = 0.35` is a no-op on synthetic data (embeddings cluster at 0.18–0.21), `GATE_ROUTER_LIMIT = 8` does all filtering. Gate 3 correctly approved archetype matches and rejected skill-emphasis mismatches.
  - **C6-real (self-use path)**: The developer's own usage IS the calibration — onboard with a real CV, run the routing pipeline against real jobs, inspect `match_queue` output via the dashboard (cosine distance, overlap score, LLM confidence, LLM reasoning all visible), and tune thresholds. The dashboard UI was built specifically as the calibration debugging interface. Synthetic seed data has been cleaned from the production database.
  - **Yield analysis (June 28 2026)**: Comprehensive analysis of job ingestion rates, match pipeline performance, and Gate 3 rejection reasons. Key findings:
    - Gate 2 threshold tightened from 0.55 to 0.48 (85% reduction in LLM calls, no loss of true positives).
    - Top rejection reasons: location mismatch (38%), wrong tech stack (24%), seniority mismatch (18%), role type mismatch (12%).
    - Persona consolidation: 3 TypeScript personas → 2 distinct + 1 new PHP/Laravel persona.
    - Seniority-aware matching + A/B test prompt variants + Gate 3 feedback loop implemented.
    - Company corpus (449 companies) identified as the primary bottleneck — **Continuous Company Acquisition Pipeline** designed through 6-iteration brainstorming session (June 29 2026). Target: 5,000 quality companies, 5-10 approved matches/day. Implementation TDD at `docs/reports/CORPUS_EXPANSION_TDD.md`, session handoff at `docs/reports/CORPUS_EXPANSION_HANDOFF.md`, brainstorming summary at `docs/governing/company-corpus-expansion-prompt.md`. Key architectural pivots: G5+G6 batch polling (Inngest 50K exec limit), G7 rawJson pruning (Neon 512MB limit), Flush-and-Flow architecture (replaces staggered queue). 10 batch sources + 13 daily-native sources, all verified for free-tier compliance. **Batch flush executed June 29 2026: 449 → 5,290 companies (106% of target).** **Corpus-persona alignment July 7 2026: 5,290 → 10,114 companies** with frontend-targeted discovery sources, company scoring, remote-scope extraction, and 5-tier circuit breaker. Top source: Wayback CDX (4,163). Two sources disabled: D1/B2 Google CSE (replaced by Brave Search API), B8 Rapid7 FDNS (replaced by crt.sh). Workable API schema drift fixed. Daily source crons (D2-D13) + v2 sources (v2FrontendJobScanner, v2GithubEventsProbe, v2FundingSignalRss) now running on staggered schedule. See `docs/reports/CORPUS_ALIGNMENT_SESSION_HANDOFF.md` for the alignment session report.
  - **⚠️ LAUNCH-BLOCKING (for public access)**: Thresholds must be benchmarked against real job/persona pairs before any non-developer user sees Module C output. A/B test prompt variants need sufficient data before declaring a winner. **D18 update (July 2026):** The Gate 2 rank-only re-architecture (`GATE2_RANK_ONLY=true`) has replaced the threshold-tuning problem with a ranking problem — the cosine cliff is eliminated, and the founder's first end-to-end run produced 2 job applications. The North Star daily report (D20) tracks the tripwire: ≥5 would-apply/day × 7 consecutive days = product-market fit found. The launch-blocking constraint is now "hit the North Star tripwire", not "tune GATE2_MAX_COSINE_DISTANCE".

### User Journey Summary
1. **Discovery**: User lands on homepage, understands value proposition `[Status: Implemented]`
2. **Conversion**: User registers/logs in via standard Auth page `[Status: Implemented]`
3. **Onboarding**: Redirected dynamically based on profile status: to `/dashboard/profile-management` for initial CV upload (if `is_onboarded=false`), or `/dashboard/jobs` if already onboarded `[Status: Implemented]` — onboarding flow (CV upload → review → profile management) and smart redirect logic on sign-in/sign-up are both implemented
4. **Engagement**: User manages CVs and profile via `/dashboard/profile-management`, reviews matched job opportunities `[Status: Implemented]` — profile management (editable: preferences, work history, personas, CV re-parse, seniority levels) is `[Status: Implemented]`; job matching backend (Module C 3-Gate funnel with seniority-aware matching, A/B test prompt variants, Gate 3 feedback loop, remote-scope extraction, company scoring, circuit breaker) is `[Status: Implemented]` (real-data calibrated via self-use yield analysis, June 28 2026; corpus-persona aligned July 7 2026 with 10,114 companies); dashboard UI for reviewing matched jobs (`/dashboard/jobs` list + `/dashboard/jobs/[matchId]` detail) is `[Status: Implemented]`
5. **Application**: User applies to jobs through ATS integration `[Status: Planned / TO DO]`
6. **Retention**: User returns to track applications and discover new opportunities `[Status: Planned / TO DO]`

## Business, Compliance & Go-To-Market (Module D) `[Status: Planned / TO DO]`

### 1. The Outbound "CTO Pitch" Strategy `[Status: Planned / TO DO]`
To secure responses in an oversaturated market, the system empowers the user to reach out with a unique position of strength:
- **"Minute Zero" Pitch**: The dashboard automatically drafts a personalized cold email template referencing the specific technology stack and architectural challenges of the matched job.
- **Peer-to-Peer Framing**: The email is written from the perspective of an engineering partner offering an "architectural chat" or "technical exchange" rather than a candidate begging for an interview. This bypasses low-level HR screenings entirely.

### 2. The Compliance 1-Pager `[Status: Planned / TO DO]`
To reduce friction when dealing with international/domestic B2B arrangements, the platform generates a professional B2B compliance sheet:
- **Independent Contracting Made Easy**: Includes pre-filled PDF/webpage resources detailing how easy it is to pay the contractor as a SaaS vendor.
- **Key Details Covered**: Explains the W-8BEN form (for non-US developers), 0% US withholding tax arrangements, and seamless setup with payment/contract platforms like Deel, Wise, or Direct Wire.
- **Procurement Alignment**: Frames the engagement in standard corporate procurement terms to facilitate quick B2B contract approvals.

### 3. Legal Protection & Terms of Service (ToS) `[Status: Planned / TO DO]`
Operating a high-throughput search and matching platform against third-party platforms requires strict legal boundaries:
- **"User-Driven Job Intelligence Tool" Framing**: The ToS explicitly defines the platform as an automated user-agent. This ensures the app acts on behalf of the individual user when reading publicly accessible data, protecting against platform-level cease and desist challenges.
- **Indemnification**: Full user indemnification clauses regarding CV accuracy and application outcomes.
- **Public Data Disclaimer**: Formally declares that all matched information is derived from publicly visible employer data feeds with no guarantees of uninterrupted access or platform affiliation.


## Hosting Infrastructure `[Status: Implemented — deployed and healthy, July 20 2026]`

Self-hosted PaaS (Hetzner Cloud CX33 + Coolify v4.1.2), chosen for flat solo-developer cost over Vercel's serverless model. Deployed on Hetzner CX33 (x86_64, Helsinki eu-central) with a 3-stage Dockerfile (Node 24-slim, standalone output, multi-arch). **VPS Postgres 17 + pgvector** (D20, July 20 2026) — the app database was migrated from Neon free tier to a self-hosted Postgres 17 in a Docker container (`z10g6zz09soe0ddwgpizteq2`) on the same Hetzner server, eliminating the Neon CU-hr burn constraint that drove the D16-D17 cron freeze. Postgres tuned: `shared_buffers=2GB`, `work_mem=16MB`, `maintenance_work_mem=512MB`, `random_page_cost=1.1`, `effective_io_concurrency=200`, `hnsw.iterative_scan=strict_order`. Local socket connection — zero network latency (was 30-50ms cross-region to Neon Frankfurt). Neon connection strings retained for disaster recovery only. Cloudflare free tier for edge protection with WAF rate-limiting on high-cost API endpoints. **Self-hosted Inngest** (Sprint 5, June 30 2026) deployed as a 3-container Docker Compose service (`inngest/inngest:v1.34.0` + `postgres:17` + `redis:7`) on the same Coolify infrastructure, accessible at `https://inngest.vectormatch.dev:8288` (port suffix required for Traefik routing — D20 closeout fix). Replaced Inngest Cloud free plan (5 concurrent steps, 50K executions/month) with unlimited self-hosted execution. **FlareSolverr 3.5.0** (D17-D20) deployed as a Coolify service for Cloudflare bypass on Wellfound, accessible at `http://flaresolverr-v104gdwm9iidiajuwd2jy52t:8191/v1` from the app container. **Backup infrastructure** (D20): nightly `pg_dump` → GCS bucket `gs://vectormatch-pg-backups`, 30-day lifecycle retention, `backupAlertHandler` in Inngest. **Resource monitoring** (D20): `scripts/ops/resource-monitor.sh` every 15min, alerts at 80% disk/RAM. **WordPress blog** (July 14 2026) deployed as a native Coolify service (`wordpress:latest` + `mariadb:11`) on the same infrastructure, served at `vectormatch.dev/blog` via Traefik reverse proxy. See *Blog & Content Architecture* above for routing details.

**Key architectural decision:** A lazy-initialization pattern is applied to `src/db/db.ts` (Neon Pool via lazy Proxy) and `src/lib/email.ts` (Resend client via `getResend()`) so that no runtime secrets are needed at Docker build time. This unblocks static generation under `cacheComponents: true` without requiring build-time `DATABASE_URL` or `RESEND_API_KEY`.

**PPR clarification:** Next.js Partial Prerendering (PPR) is fully supported under standalone Docker output with `cacheComponents: true` — the production build confirms all routes render as `◐ (Partial Prerender)`. The trade-off is that PPR pages cannot be served from a CDN edge (every request reaches the live NextServer process), not that PPR is unavailable.

Full rationale, cost model, deployment workflow, lazy-init pattern, and security checklist are documented in **TDD Module E** (Sections 7.1–7.6) — not duplicated here to avoid the two documents drifting apart on decisions this consequential.